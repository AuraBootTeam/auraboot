---
type: backlog
status: active
created: 2026-06-11
relates_to:
  - docs/backlog/2026-06-11-rule-center-integration-coverage-gap.md
  - docs/superpowers/specs/2026-06-05-automation-designer-golden-e2e-design.md
---

# 全量设计器笛卡尔积覆盖矩阵方案

## 目标

本 scope 不是新增业务功能，而是对 AuraBoot 现有拖拽 / 画布 / 设计器类页面做一轮独立的测试真实性治理与补测。目标是把 Page Designer、BPMN Designer、Automation Designer、Dashboard / Report Designer 的“组件类型 × 属性面板 × 结构操作 × 按钮 × 保存回显 × 导入导出 × 后端消费”矩阵列清楚，并对 P0/P1 缺口补真实浏览器和后端证据。

完成后只能声明“矩阵中标记为 DONE 的行已通过证据闭环”。禁止把一个代表性 happy path、旧 deep suite pass count、或者 visible-only 按钮断言包装成“全量设计器已完整覆盖”。

## 为什么要单独开新 scope

规则中心收口文档已经关闭了规则 / SLA / BPM / Automation / Permission / EventPolicy 的 targeted slice，但全量设计器矩阵属于另一类工作：

- 设计器组件和属性组合数量大，和规则中心不是同一个验收边界。
- Page / BPMN / Automation / Dashboard 复用部分 shared kernel，但各自消费方的 palette、属性 schema、保存 payload、导出 artifact 和 runtime 语义不同，不能只测 shared kernel demo。
- 旧 E2E 中仍有历史债：`test.skip`、`test.fixme`、`waitForTimeout()`、visible-only、API PUT 绕 UI、宽松阈值等。需要逐个判断是产品缺口、环境债、已被新 spec 覆盖，还是应该直接退役。
- 笛卡尔积不能机械爆炸式穷举。正确做法是先 inventory，再分层抽样和风险分级，把高风险组合补到真实闭环。

## 范围

### In Scope

- Page Designer / Unified Page Designer
- BPMN Designer / BPM Flow Designer
- Automation Designer
- Dashboard Designer / Report Designer
- 共享拖拽内核相关 helper、test id、E2E harness，但只在消费方矩阵需要时修改
- 设计器导入 / 导出 artifact：文件名、扩展名、header、内容解析、round-trip
- 保存 / 发布 / 部署 / 启用 / 预览 / 运行类按钮的真实 UI 触发与后端反查
- 负向路径：必填缺失、非法嵌套、非法连线、非法 schema、无权限、后端校验失败、导入非法文件

### Out of Scope

- 新写整页 React 设计器。除非已有平台能力无法表达且用户确认，否则默认复用平台 DSL / custom block / 既有设计器内核。
- 重做拖拽内核。优先复用 page-designer、BPMN designer、automation designer、dashboard designer 的 palette/store/canvas/selection/undo-redo 能力。
- 无业务价值的数学式全组合穷举。低风险样式属性可按组件族抽样，高风险属性必须逐项闭环。
- 生产部署。此 scope 只负责本地真栈 / CI 可复现的测试与代码收口。

## 完成判定

完成声明前必须同时满足：

1. 每个设计器都有 feature/action inventory。
2. 每个设计器都有 designer coverage matrix，所有 P0/P1 行状态为 `DONE / N/A / WONT_DO`，不能有 `unknown / draft / ❌ / ⚠️ 语义浅`。
3. 每个 DONE 行有 browser evidence 和 backend evidence；导出 / 下载行还必须有 artifact evidence。
4. 每个保存类动作必须证明 UI 操作触发真实 API，保存后 API/DB 反查一致，reload 后画布与属性回显一致。
5. 每个发布 / 部署 / 启用 / 运行类动作必须证明后端 runtime 或下游消费语义，不只看 toast。
6. `/e2e-feature-coverage` 思路矩阵已更新，`/e2e-truth` 红线 grep 已跑，新增或本轮引用证据中不含 skip/fixme/fixed wait/retry/PUT 绕 UI/visible-only 假覆盖。
7. 文档回填每轮测试命令、结果、剩余风险。

## 统一矩阵模板

每个设计器至少维护一张矩阵：

| 设计器 | 组件族 / 节点族 | 具体类型 | 添加方式 | 属性面板字段 | 结构操作 | 保存/发布/运行 | Reload 回显 | 导入/导出 | Browser evidence | Backend evidence | Artifact evidence | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| page-designer | table | data-table | palette drag | columns / dataSource / rowActions | drag / move / delete | save / publish / preview | schema reload | page.json export/import | spec + screenshot | `/api/pages/{pid}` | `{pageKey}.page.json` parsed | TODO |

状态枚举：

- `TODO`: 未开工。
- `IN_PROGRESS`: 正在补测或修产品问题。
- `DONE`: browser + backend + artifact/runtime 证据齐备。
- `N/A`: 当前设计器不支持该动作，且有代码或产品文档证据。
- `WONT_DO`: 明确不做，必须写原因和替代证据。

## Page Designer 矩阵

### 组件族

| 组件族 | 代表类型 | 必测属性 | 必测动作 | 后端 / artifact 证据 | 状态 |
|---|---|---|---|---|---|
| layout/container | section / columns / tabs | title、layout、children | 拖入、嵌套、移动、删除、撤销/重做 | page schema blocks 顺序 | PARTIAL: 2026-06-12 `showcase/detail-all-blocks-e2e.spec.ts` 新增 `D.layout-tabs`,覆盖 detail runtime 中 `tabs[].layout` / `tabs[].blocks[].layout` schema readback,浏览器从 sidebar/list row 打开 detail 后断言 overview tab 的 `grid-layout`、`grid-item-*`、nested `detail-section` + `rich-text`,切到 secondary tab 后断言 `stack-layout`、`divider` + nested `rich-text`,并确认 inactive tab content unmount。仍不声明 Page Designer palette 的 container 嵌套拖拽、移动、删除、撤销/重做和属性面板组合全覆盖 |
| form/input | form-section / text / number / reference / select / date | field binding、required、readonly、visibleWhen | 添加字段、编辑属性、required 空提交 | `/api/pages/{pid}` + 表单运行页校验 | PARTIAL: 2026-06-12 `standard-blocks-runtime.spec.ts` 覆盖 custom form 中 form-section 通用 field renderer:空 required submit 显示 field error 且不触发 `pgm:create_page_schema`,readonly 字段保持 `readOnly=true` 与默认值,`visibleWhen` 随 `form.name` 输入出现;仍不声明 text/number/reference/select/date 每个组件类型和属性面板组合全覆盖 |
| table/list | data-table / smart-table | columns、dataSource、rowActions、filters | 列配置、搜索、行操作、批量操作 | schema + 运行页行数据 + batch API/DB 反查 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 已补 custom list runtime search、row action navigation、bulk toolbar/modal;2026-06-12 追加 bulk edit/delete DB side effects,真实浏览器选择行、Bulk Edit 提交、`PUT /api/dynamic/page_schema/batch` 成功、`/api/pages/key/{pageKey}` 读回 name 变更,随后选择另一行 Delete、确认弹层、`DELETE /api/dynamic/page_schema/batch` 成功并用 list API 反查目标行消失、host 页仍存在 |
| sub-table | foreignKey / resolveVia / dataSource | childModel、foreignKey、inlineEdit | 新增子行、编辑、保存、取消 | API/DB 子表反查 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 复用真实 `e2et_order_form` runtime,浏览器新增/编辑/删除 `e2et_order_item`,API 反查子行 `subtotal` 与父单 `amount/qty`;本 slice 同时修复后端 `computedFields` partial update 未合并当前记录快照导致 update 后 subtotal/aggregate stale 的产品缺口。2026-06-12 `showcase/subtable-modes-e2e.spec.ts` 在导入 `plugins/showcase` 后覆盖 sub-table 三种数据模式:foreignKey、resolveVia、dataSource;浏览器从侧边栏进入 list/detail,替换 `showcase_all_fields_detail` blocks 后分别断言子表 title、行数据、seeded `sc_name/sc_code` 渲染 |
| chart/stat | stat-card / chart | query/dataSource、metric field | 配置、预览、刷新 | NQ/API records 非空 | TODO |
| custom block | typed custom block | props schema、valueField | 配置、保存、运行页渲染 | block props + runtime output | PARTIAL: 2026-06-12 `standard-blocks-runtime.spec.ts` 覆盖 custom-only list host 发布页: `/api/pages/key/{pageKey}` 读回 `blockType:"custom"`、组件别名 `decision-field-impact`、`visibleWhen:${false}` 与 unresolved component 配置,真浏览器 `/p/c/{pageKey}` 渲染 `DecisionFieldImpactBlock` runtime output,隐藏条件只保留 1 个 runtime block,未知组件显示 `Failed to load E2EMissingCustomRuntimeBlock` 而非 silent null。仍不声明所有 typed custom block 的 props schema、valueField、属性面板保存语义全覆盖 |
| upload/attachment | upload block | accept、maxSize、field binding | 上传、下载、删除 | 文件 API + artifact header | TODO |

### 现有证据起点

- `web-admin/tests/e2e/page-designer/designer-deep-operations.spec.ts`
- `web-admin/tests/e2e/page-designer/page-designer-full-lifecycle.spec.ts`
- `web-admin/tests/e2e/page-designer/field-properties.spec.ts`
- `web-admin/tests/e2e/page-designer/smart-components.spec.ts`

第一轮需要先审这些 spec 是否仍有 skip、fixed wait、宽松阈值或只测 visible。

## BPMN Designer 矩阵

### 组件族

| 组件族 | 具体类型 | 必测属性 | 必测动作 | 后端 / runtime 证据 | 状态 |
|---|---|---|---|---|---|
| event | startEvent / endEvent | initiator、formKey、terminateAll | 添加、选择、属性编辑 | designerJson + BPMN XML | TODO |
| task | userTask | assigneeType/value、approvalMode、priority-like task config、skipable、formBinding | 添加、属性编辑、保存、deploy、runtime task | DTO + XML + task runtime | TODO |
| service task | command / http / java / notification / rule | serviceType、commandCode、http config、rule config | 保存、deploy、runtime执行 | XML delegate + runtime side effect | TODO |
| receiveTask | receiveTask | name / message config if supported | 保存、deploy | XML receiveTask | TODO |
| gateway | exclusive / parallel / inclusive | defaultFlow、condition builder | 连线、条件、非法缺条件 | XML conditionExpression + runtime branch | TODO |
| callActivity | callActivity | calledProcessKey、input/output mapping | parent/child deploy、runtime | XML calledElement + child task | TODO |
| edge | sequenceFlow | label、condition、sourceHandle | 连线、改条件、删除 | designerJson edge + XML | TODO |

### 现有证据起点

- `web-admin/tests/e2e/designer/designers.spec.ts`
- `web-admin/tests/e2e/bpm-designer/designer-node-property-matrix.spec.ts`
- `web-admin/tests/e2e/bpm-designer/designer-receivetask.spec.ts`
- `web-admin/tests/e2e/bpm-designer/designer-gateway-condition.spec.ts`
- `web-admin/tests/e2e/bpm-designer/designer-servicetask-command.spec.ts`
- `web-admin/tests/e2e/bpm-designer/designer-servicetask-http.spec.ts`
- `web-admin/tests/e2e/bpm-designer/designer-callactivity.spec.ts`
- `web-admin/tests/e2e/bpm-designer/designer-usertask-assignee-matrix.spec.ts`

已退役的旧套件不能作为证据：

- `web-admin/tests/e2e/designer/bpmn-node-properties.spec.ts`
- `web-admin/tests/e2e/designer/bpmn-designer-deep.spec.ts`

## Automation Designer 矩阵

### 组件族

| 组件族 | 具体类型 | 必测属性 | 必测动作 | 后端 / runtime 证据 | 状态 |
|---|---|---|---|---|---|
| trigger | record-create / record-update / field-change / state-change / scheduled / webhook / bpm-event / inactivity | modelCode、event、condition、ruleBinding、inactivity filter | 拖入、配置、保存、enable、fire / scheduler sweep | automation DTO + execution log | DONE: trigger family covered in `automation-designer-golden.spec.ts` including 2026-06-12 `trigger-bpm-event` and `trigger-inactivity` |
| action | update-record / create-record / send-notification / execute-command / call-api / send-webhook / start-process / llm-call | target model、field mapping、payload、receiver、fallback | 保存、test run、debug step | side effect + node statuses | DONE: action family covered in `automation-designer-golden.spec.ts` including 2026-06-12 `action-start-process` |
| control | condition / loop / delay | expression、branch condition、collection mapping、timer | true/false branch、非法表达式、循环边界 | SmartEngine process + logs | DONE: condition/loop/delay runtime semantics covered; delay seam added 2026-06-12 |
| edge | true/false/default branch | conditionExpression、sourceHandle | 连线、改条件、删除 | flowConfig edges + runtime branch | DONE: true/false/default branch routing covered by condition/loop/action golden flows |
| rule binding | decision ref block | decisionCode、versionPolicy、input mapping、fallback | 配置、保存、impact graph | usage-index + impact | DONE in rule-center slice |

### 现有证据起点

- `web-admin/tests/e2e/automation/automation-designer-golden.spec.ts`
- `web-admin/tests/e2e/automation/automation-golden.spec.ts`
- `web-admin/tests/e2e/automation/rule-binding-designer-host.spec.ts`
- `web-admin/tests/e2e/designer/automation-designer-deep.spec.ts`
- `web-admin/tests/e2e/automation/automation-validation-gate.spec.ts`
- `web-admin/tests/e2e/automation/llm-call-node.spec.ts`

第一轮建议先审 `automation-designer-golden.spec.ts` 的 fixed wait 和阈值断言，区分：

- 合理业务下限
- runtime polling 可替代的 sleep
- 真实产品缺口
- 已由后端集成覆盖但 E2E 不应宣称 UI 覆盖的项

## Dashboard / Report Designer 矩阵

### 组件族

| 组件族 | 具体类型 | 必测属性 | 必测动作 | 后端 / artifact 证据 | 状态 |
|---|---|---|---|---|---|
| widget | KPI / table / chart / text / markdown / iframe if supported | title、dataSource、refresh、style | 添加、编辑、删除、复制、排序 | dashboard config | PARTIAL: Dashboard 37-widget add/saved-payload + exhaustive `configSchema` field presence matrix DONE; CRM workbench stage/status/type variants + permission negative states DONE; runtime semantics remain partial by widget family |
| dataSource | static / API / namedQuery / model | query、params、field mapping | 预览、保存、刷新 | API response / records | TODO |
| layout | grid / tabs / responsive layout | size、position、breakpoints | drag resize、reorder、reload | config layout | TODO |
| export | Excel / PDF / JSON if supported | file naming、content | download、parse、round-trip where supported | xlsx/pdf/json artifact | PARTIAL: Dashboard Excel/PDF done; Report table + non-table static Excel/PDF artifacts done; Report model/namedQuery/API Excel dataSource artifact done; Report JSON round-trip artifact done; Report PDF page-settings/text-hierarchy fidelity done; richer chart/barcode/watermark visual-equivalence is not claimed |
| publish/share | global/personal/dashboard visibility | permission、publish state | publish/unpublish、reload | API state + permission | TODO |
| management list | row actions / list tabs | row action visibleWhen、onRowClick detailUrl、scope tabs | filter、publish/unpublish/delete、row navigation、tab filter | dashboard API state + browser reload | DONE: 2026-06-12 `dashboard-management.spec.ts` fresh run covers row action visibility, publish/unpublish/delete lifecycle, row/edit/create navigation, and All/Personal/Global tabs |

### 现有证据起点

- `web-admin/tests/e2e/dashboard/dashboard-export.spec.ts`
- `web-admin/tests/e2e/dashboard/dashboard-designer-deep.spec.ts`
- `web-admin/tests/e2e/dashboard/dashboard-widget-types.spec.ts`
- `web-admin/tests/e2e/dashboard/dashboard-charts.spec.ts`
- `web-admin/tests/e2e/dashboard/dashboard-interactions.spec.ts`
- `web-admin/tests/e2e/dashboard/dashboard-management.spec.ts`
- `web-admin/tests/e2e/designer/report-designer-deep.spec.ts`

已知已关闭：

- Excel export artifact：文件名、ZIP header、workbook sheet/rows。
- PDF export artifact：文件名、PDF header、page marker/content marker。
- Report table Excel artifact：`extension.reportDsl` 保存、BFF binary proxy、下载文件名、ZIP header、workbook sheet/header/rows。
- Report table PDF artifact：`extension.reportDsl` 保存、后端 PDFBox 渲染、下载文件名、PDF header、page marker、PDFTextStripper 内容解析。
- Report non-table static Excel/PDF artifact：`grouped-table` 分组行、`cross-tab` pivot 矩阵、`stat-card` 聚合值、`rich-text` 段落、`chart` 聚合数据表、`barcode`/`watermark`/`page-header`/`page-footer` 文本 artifact。
- Report model/namedQuery/API Excel dataSource artifact：model filter rows、namedQuery params rows、canonical `/api/datasource(s)/list` + `datasourceId=nq:{queryCode}` rows,并用 XLSX 三 sheet 解析证明 byte-level content。
- Report JSON round-trip artifact：保存的 `extension.reportDsl`、`format=auraboot.report.export.v1`、`reportPid`、完整 `reportDsl`、resolved `dataSets` rows,并用浏览器下载 JSON parse 证明 model/namedQuery/API rows。
- Report PDF page-settings/text-hierarchy fidelity：`page.size/orientation/margin(mm)` 进入 PDF MediaBox/坐标,标题/块标题字号层级进入 PDF content stream,并由 PDFBox + browser download 双证据解析。

仍需审查：

- widget types spec 的 `>=1` 假覆盖已关闭:2026-06-12 `dashboard-widget-types.spec.ts` 改为精确 palette 清单、add 后 widget 数量 `+1`,并在画布上用 `data-widget-type` 断言实际新增 widget 类型;`DW-037..DW-073` 覆盖当前 dashboard inventory 37 个 widget 的 UI 添加、static dataSource 编辑、代表属性编辑、Save API、`/api/dashboards/{pid}` 读回、exact `type/componentType/default size`。
- registry / workbench widget 的 saved-payload representative matrix 已覆盖到 37 个类型;2026-06-12 `DWR-001` / `DWR-004` / `DWR-005` / `DWR-006` / `DWR-007` / `DWR-008` / `DWR-009` 先补 22 个代表 widget runtime visual/semantic/live-data evidence,`DWR-010` / `DWR-011` / `DWR-012` / `DWR-013` 补 CRM workbench navigation、empty/unavailable、stage/status/type frontend variant matrix 和 permission-denied negative states,`DWR-014` 再补剩余 13 个 foundational chart widget 的 published viewer runtime surface/DOM,因此 37 个类型 runtime render path 现在均有浏览器证据。
- 37 个 widget 现在已有 representative saved-payload / property value matrix;2026-06-12 新增 `DW-074-01..37` registry-driven exhaustive `configSchema` field presence matrix,覆盖当前 37 个 widget 的 170 个属性面板字段,并验证 `dataSource.type` 的 aggregate/namedQuery `dependsOn` 显示/隐藏分支。该 DONE 只证明属性面板字段可见性与条件分支,不能外推为每个字段的保存语义或 runtime 语义全覆盖。
- Report model/namedQuery/API Excel dataSource artifact、JSON round-trip artifact、PDF page-settings/text-hierarchy fidelity 已补;仍不能外推为 PDF chart/barcode/watermark 像素级视觉等价 DONE。

## 红线审查命令

新窗口启动后先跑这些命令，不要先写代码：

```bash
cd /Users/ghj/work/auraboot/auraboot/.worktrees/decisionops-console-completion

rg -n 'test\.skip|test\.fixme|waitForTimeout\(|toBeGreaterThanOrEqual\(|toBeLessThanOrEqual\(|retries:|waitForEvent\(["'\'']download["'\'']\)\.catch|page\.request\.put|__reactProps' \
  web-admin/tests/e2e/page-designer \
  web-admin/tests/e2e/designer \
  web-admin/tests/e2e/bpm-designer \
  web-admin/tests/e2e/automation \
  web-admin/tests/e2e/dashboard \
  -g '*.spec.ts'
```

然后按文件分类：

| 分类 | 处理 |
|---|---|
| 新证据链要引用的 spec | 必须修掉 skip/fixme/fixed wait/PUT 绕 UI/visible-only |
| 已被更强 spec 覆盖的旧 spec | 退役删除或降级为 smoke，文档写替代证据 |
| 环境前置型 skip | 优先修 seed / import / permission fixture；不能修则不计入完成证据 |
| 产品真实缺口 | 新建矩阵行，补产品能力 + browser/backend 测试 |
| 合理业务下限 | 保留，但加注释说明语义，不能用于核心完成比例 |

## 实施顺序

### Phase 0: 新 goal 与环境校准

建议新窗口第一条 goal：

```text
/goal 完成全量设计器笛卡尔积覆盖矩阵第一轮: 产出 inventory + coverage matrix, 清理本轮引用证据中的假覆盖, 对 P0/P1 缺口补真实 browser/backend/artifact 测试, 提交并 push
```

启动前校准：

- `git status --short`
- `git branch --show-current`
- `git pull --rebase`
- `curl --noproxy '*' http://127.0.0.1:6443/actuator/health`
- `curl --noproxy '*' -I http://127.0.0.1:5174/home`
- 确认 backend、Vite/BFF、admin storage state 可用。

### Phase 1: Inventory

提取每个设计器的：

- palette/node/widget 类型
- 属性面板字段
- toolbar/context menu 按钮
- save/publish/deploy/enable/test-run/preview/export/import API
- artifact 类型
- runtime 消费方

输出到本文档新增章节“Inventory Snapshot”，不要只写口头结论。

## Inventory Snapshot

> 2026-06-11 live source extraction. Source of truth is implementation code, not old pass counts.

### Extraction Evidence

| 范围 | 真源 | live 计数 |
|---|---|---:|
| Page Designer block palette | `web-admin/app/plugins/core-designer/components/studio/workbench/designers/areas/BlockLibrary.tsx` | 17 |
| BPMN Designer palette | `web-admin/app/plugins/core-designer/components/bpmn-designer/constants/index.ts` | 9 |
| Automation Designer palette | `web-admin/app/framework/smart/automation/nodes/{triggers,actions,controls}.ts` | 19 |
| Dashboard Designer widget registry | `web-admin/app/plugins/core-dashboard/widgets/widgetRegistry.ts` | 37 |
| Report Designer block palette | `web-admin/app/plugins/core-designer/components/report-designer/components/BlockPalette.tsx` | 10 |

### Page Designer Inventory

| 类型 | 分类 | 可用页面 | P0/P1 风险 | 本轮证据状态 |
|---|---|---|---|---|
| filters | form | list | P1: filter-form 输入、保存、运行页过滤 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 发布真实 custom list 页,浏览器打开 `/p/c/{pageKey}`,点击 `filters-toggle`,断言 `search-area`、`field-name`、search/reset 按钮 |
| form-section | form | form/list | P0: 字段绑定、required、readonly、visibleWhen | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 发布真实 custom form 页,断言 `dynamic-form`、section title、`form-field-name/page_key` 与 runtime field renderer;2026-06-12 追加 required/readonly/visibleWhen 语义 targeted 覆盖:空 required submit 被 field validation 拦截且不发 command,readonly 默认值不可编辑,conditional 字段随 `form.name` 出现;多组件类型深度仍归 form/input 族矩阵 |
| form-buttons | form | form | P0: create/update 命令真实触发 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 断言 `form-btn-submit/cancel`,并点击 configured `navigateTo` cancel 证明 form-buttons action dispatch;2026-06-12 追加 form command side effects,真实浏览器在 custom form 点击 `Runtime save` 触发 `pgm:create_page_schema`,后端 `/api/pages/{pid}` 读回新页面 name/pageKey,再打开 seeded `page_schema_form` edit URL 点击 submit 触发 `pgm:update_page_schema`,后端读回 name 更新 |
| table | display | list | P0: columns/dataSource/rowActions/search/bulk | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 断言 published list 页中的 `ab:list:page_schema:table` 与 `table-cell-0-name/status`,证明 table block 被运行页消费并加载真实 list data;2026-06-12 新增 custom list runtime 搜索唯一 `page_key`,断言 `table-cell-0-name/page_key`,点击 `row-action-runtime_inspect` 证明 table `rowActions` 进入 runtime action navigation 模板,勾选 row checkbox 后断言 bulk toolbar `Edit/Delete/Clear selection` 与 Bulk Edit modal;2026-06-12 追加 bulk edit/delete DB side effects,覆盖 browser click -> dynamic batch PUT/DELETE -> API 读回/反查 |
| detail-section | display | form/list | P1: 详情字段回显、raw code 防泄漏 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 断言 `.block-detail-section` title 与 field label;同时产品修复 `DetailPageContent` 允许 `detail-section` 走 direct form block 渲染 |
| text | display | list/form | P2: 静态内容保存回显 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 用 `props.content` 发布 `text`,运行页 `.block-text` 渲染 sanitized HTML 内容 |
| toolbar | layout | list | P0: action.command 与命令管道 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 断言 list header `toolbar-btn-runtime_ping` 来自 published toolbar block;命令副作用深度仍归 action.command 矩阵 |
| selection-info | layout | list | P1: 多选状态与批量工具条 | DONE: 2026-06-12 新增 `SelectionInfoBlockRenderer`;runtime E2E 先断言 count `0`,点击 helper action 写 `selectedRows`,再断言 count `1` 与 selected row label |
| stat-card | chart | list/form | P1: NQ/API 非空与数值断言 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 用 schema static `ds_stats`,断言 `stat-card-value=42`、suffix `items`、trend `+12%` |
| chart-card | chart | list/form | P1: 图表数据源与渲染 | DONE: 2026-06-12 `standard-blocks-runtime.spec.ts` 用 `props.chartType/xField/yField` alias + static chart dataSource,断言 `.block-chart-card` 渲染 canvas/svg 且无 unknown/unsupported block 文案 |
| metric-strip | workbench | list/form | P1: 指标点击筛选联动 | DONE: 2026-06-12 `workbench-blocks-runtime.spec.ts` 在真实 `/p/c/{pageKey}` custom list 页发布 schema, readback `dataSources`, 渲染静态 metric 数据, 点击 `pending` 写 `state.reviewMode` 并驱动 action-bar 可见性 |
| record-inspector | workbench | list/form | P1: 选中行联动详情 | DONE: 2026-06-12 runtime E2E 先断言空态,再通过 `candidate-list` 选中 `C-100` 写入 runtime state, `record-inspector` 渲染 title/status/evidence source |
| candidate-list | workbench | list/form | P1: 候选选择写回状态 | DONE: 2026-06-12 runtime E2E 用 schema-level static `ds_candidates`,断言候选行与 detail fields,点击候选写 `selectedWorkbenchRecord` 并驱动 inspector/evidence/drawer |
| workbench-action-bar | workbench | list/form | P0: 导出/下载/命令行动点 | DONE: 2026-06-12 runtime E2E 覆盖 `visibleWhen` 初始隐藏、metric click 后显示 `pending_only`,点击 `mark_reviewed` 后显示 `reviewed_only`;同时补 `/p/c/:pageKey` route manifest 单测避免 custom page 落入菜单 catch-all |
| evidence-panel | workbench | list/form | P1: raw payload/证据渲染 | DONE: 2026-06-12 runtime E2E 先断言空态,选中候选后渲染 conflict 与 JSON raw payload (`supplier-audit.xlsx`) |
| artifact-timeline | workbench | list/form | P1: 附件/导出产物历史 | DONE: 2026-06-12 runtime E2E 用 schema-level static `ds_artifacts`,断言 filename/revision/hash 行与 `/api/file/download/file-workbench-100` href |
| review-drawer | workbench | list/form | P1: 浮层复核、候选确认 | DONE: 2026-06-12 runtime E2E 先断言空态,选中候选后渲染 floating drawer title、score badge、compare tab、candidates tab;覆盖 state/context/dataSource 三路联动 |

### Automation Designer Inventory

| 类型 | 分类 | 属性字段 | P0/P1 风险 | 本轮证据状态 |
|---|---|---|---|---|
| trigger-record-create | trigger | modelCode | P0: create event fire | DONE: golden flow fires real create event and verifies log + downstream side effect |
| trigger-record-update | trigger | modelCode, watchFields | P0: update event fire / create 不误触发 | DONE: golden flow fires real update event and verifies update-specific log + side effect |
| trigger-field-change | trigger | modelCode, fieldCode, fromValue, toValue | P0: watched field 与 non-watched field | DONE: golden flow covers watched field-change and non-watched no-fire behavior |
| trigger-state-change | trigger | modelCode, stateField, fromStates, toStates | P0: dict-backed state filter | DONE: golden flow covers dict-backed state transition fire |
| trigger-scheduled | trigger | cron, timezone, maxExecutionTime | P1: scheduler fire / no fixed sleep fake pass | DONE: golden flow covers scheduler fire without fixed sleep evidence |
| trigger-webhook | trigger | secret, validationMode, expectedHeaders | P0: inbound webhook real POST | DONE: golden flows fire real inbound webhook POSTs and verify execution logs |
| trigger-bpm-event | trigger | modelCode, eventTypes | P1: BPM event consumer seam | DONE: 2026-06-12 `N-TRIGGER-BPM-EVENT` browser/backend runtime seam, static multiselect saves `eventTypes` as an array, bridge subscribes to EventBusService internal BPM events, SmartEngine versioned process key matches bare automation processKey, BPM `task_assigned` creates downstream order item |
| trigger-inactivity | trigger | modelCode, inactivityHours, inactivityField, stateField, inactivityStates | P1: scheduled inactivity sweep | DONE: 2026-06-12 `N-TRIGGER-INACTIVITY` browser/backend runtime seam, advanced property group is expandable via stable selector, `inactivityField/stateField/inactivityStates` persist as backend field/dict codes, configurable scheduler sweep finds stale cancelled record and creates downstream order item |
| action-update-record | action | modelCode, recordId, fields | P0: side effect + sad invalid field | DONE: golden flow verifies persisted field update and sad invalid field failure |
| action-create-record | action | modelCode, fields | P0: child record created + invalid field sad | DONE: golden flow verifies child record creation and invalid field failure |
| action-send-notification | action | notificationType, title, content, recipients | P1: notification runtime completion | DONE: golden flow verifies notification node completion |
| action-execute-command | action | commandCode, params | P0: command-select picker + restricted principal sad | DONE: golden flow verifies command-select save and restricted-principal sad path |
| action-call-api | action | url, method, headers, body | P0: outbound success/failure | DONE: golden flow verifies outbound success and 404 sad node failure |
| action-send-webhook | action | url, payload | P0: outbound receiver payload + 500 sad | DONE: golden flow verifies receiver payload and upstream 500 sad behavior |
| action-start-process | action | processKey, businessKey, variables | P1: BPM runtime side effect | DONE: 2026-06-12 `N-START-PROCESS` browser/backend runtime seam, process picker uses deployed BPM definitions, Automation `CUSTOM` storage switches to BPM `DATABASE`, businessKey status proves BPM instance |
| action-llm-call | action | model, prompt fields, outputVariableName, imageVariableNames | P1: built-in stub provider + persisted output; external key not required | DONE: golden flow verifies built-in stub provider output without external key |
| control-condition | control | expression | P0: true/false/edge boundary | DONE: golden flows cover true/false branch routing and invalid-expression sad path |
| control-delay | control | duration, unit | P1: delayed runtime semantics | DONE: 2026-06-12 `N-DELAY` browser/backend runtime seam, compiler maps to `delay`, executor consumes `duration/unit` |
| control-loop | control | collection, itemVariable | P1: non-empty and empty collection | DONE: golden flows cover non-empty loop fan-out and empty collection behavior |

### Automation Runtime Architecture Notes

> 2026-06-12 收口 `trigger-bpm-event` 时补充。以下内容只解释运行时边界,不带浏览器 / 后端验证行时不能单独计入 DONE 证据。

#### Spring BPM event 与 SmartEngine 的关系

`Spring BPM event` 不是 SmartEngine 的独立模块,而是本次调试里对 `BpmEvent` 经 Spring `ApplicationEventPublisher` 发布的简称。AuraBoot BPM 层接收或创建运行时事件后,会把它包装成 `BpmEvent`,再通过本地事件分发层交给通知、SLA、Automation 等消费方。

运行时链路是:

```text
SmartEngine BPM execution/task event
  -> AuraBoot BpmEvent / EventBusService
  -> Spring ApplicationEvent + internal BPM subscribers
  -> BpmEventAutomationBridge
  -> AutomationTriggerService.onBpmEvent
  -> matched automation execution
```

这个边界会直接影响验证方式:

- SmartEngine 是 BPM 执行引擎,负责启动流程、创建任务,并通过 AuraBoot 集成点发出任务生命周期事件。
- `EventBusService` 是 AuraBoot 的 BPM 事件分发层,负责按需持久化任务事件、派发 internal subscriber,并发布 Spring event。
- `BpmEventAutomationBridge` 是 BPM event 进入 Automation 的消费 seam。单独证明 SmartEngine 能启动流程不等于证明这个 bridge 可用;E2E 必须证明某个 BPM event 能产生 automation log 和下游副作用。

当前源码锚点:

- SmartEngine process start: `platform/src/main/java/com/auraboot/framework/bpm/service/ProcessEngineService.java`
- BPM event dispatch: `platform/src/main/java/com/auraboot/framework/bpm/event/EventBusService.java`
- Automation bridge: `platform/src/main/java/com/auraboot/framework/bpm/listener/BpmEventAutomationBridge.java`

#### AutomationTriggerService 与 SmartEngine 的关系

`AutomationTriggerService` 负责触发匹配,不负责图编排。它接收 record / BPM / webhook / scheduled 等事件,加载启用的 automation,按 trigger config 过滤,评估 trigger condition 和 decision binding,然后把命中的 automation 派发给运行时执行。

当前执行设计:

```text
AutomationTriggerService
  -> AutomationProcessRuntime.run()
  -> AutomationFlowCompiler.compile()
  -> deploy/start auto_<automationPid> SmartEngine process
  -> SmartEngine orchestrates sequence/gateway/loop
  -> AutomationActionServiceTaskDelegate
  -> CompositeActionExecutor
  -> concrete action executor
```

准确表述是:Automation 的触发匹配在 AuraBoot application service 中完成;automation graph 的执行会编译并交给 SmartEngine 编排;具体业务动作仍由 AuraBoot 的 `ActionExecutor` 实现执行。

当前源码锚点:

- Trigger matching and log lifecycle: `platform/src/main/java/com/auraboot/framework/automation/trigger/impl/AutomationTriggerServiceImpl.java`
- Automation flow compile/deploy/run: `platform/src/main/java/com/auraboot/framework/automation/bpm/AutomationProcessRuntime.java`
- Designer graph to SmartEngine shape: `platform/src/main/java/com/auraboot/framework/automation/bpm/AutomationFlowCompiler.java`
- SmartEngine serviceTask to action executor bridge: `platform/src/main/java/com/auraboot/framework/automation/bpm/AutomationActionServiceTaskDelegate.java`

### BPMN Designer Inventory

| 类型 | 分类 | P0/P1 风险 | 本轮证据状态 |
|---|---|---|---|
| START_EVENT | event | initiator / XML start event | TODO |
| END_EVENT | event | terminateAll / XML end event | TODO |
| USER_TASK | task | assignee matrix / form binding / MI / runtime task | TODO |
| SERVICE_TASK | task | command / http / runtime side effect | TODO |
| RECEIVE_TASK | task | designerJson + BPMN XML | TODO |
| EXCLUSIVE_GATEWAY | gateway | condition + default branch + runtime route | TODO |
| PARALLEL_GATEWAY | gateway | fork/join no conditionExpression | TODO |
| INCLUSIVE_GATEWAY | gateway | multi-condition branch runtime | TODO |
| CALL_ACTIVITY | task | parent/child deploy + variable mapping runtime | TODO |

### Dashboard / Report Designer Inventory

| 范围 | 类型 | 属性字段摘要 | P0/P1 风险 | 本轮证据状态 |
|---|---|---|---|---|
| dashboard | smart-number-card | title, icon, suffix, showTrend | P0: add/save/reload + data source | DONE(saved-payload + runtime): 2026-06-12 `DW-038` proves UI click-add, static dataSource edit, representative `visualization.suffix` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and asserts `smart-number-card` renders the static KPI value `1,234 units`. |
| dashboard | smart-bar-chart | title, horizontal, stacked | P0: add/save/reload + data source | DONE(saved-payload + runtime): 2026-06-12 `DW-039` proves UI click-add, static dataSource edit, representative `visualization.horizontal` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-bar-chart`. |
| dashboard | smart-line-chart | title, smooth, showArea | P0: add/save/reload + data source | DONE(saved-payload + runtime): 2026-06-12 `DW-040` proves UI click-add, static dataSource edit, representative `visualization.showArea` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-line-chart`. |
| dashboard | smart-pie-chart | title, donut, showLabels | P0: add/save/reload + data source | DONE(saved-payload + runtime): 2026-06-12 `DW-041` proves UI click-add, static dataSource edit, representative `visualization.donut` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-pie-chart`. |
| dashboard | smart-area-chart | title, smooth, fillOpacity | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-037` proves UI click-add, static dataSource edit, representative `visualization.fillOpacity` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, default size, and registry `visualization.smooth` persistence. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-area-chart`. |
| dashboard | smart-funnel-chart | title, sort | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-042` proves UI click-add, static dataSource edit, representative `visualization.sort` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-funnel-chart`. |
| dashboard | smart-scatter-chart | title, bubbleMode | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-043` proves UI click-add, static dataSource edit, representative `visualization.bubbleMode` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-scatter-chart`. |
| dashboard | smart-radar-chart | title, shape, showArea | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-044` proves UI click-add, static dataSource edit, representative `visualization.shape` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-radar-chart`. |
| dashboard | smart-table-chart | title, pageSize, striped | P0: table rows + data source | DONE(saved-payload + runtime): 2026-06-12 `DW-045` proves UI click-add, static dataSource edit, representative `visualization.pageSize` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and asserts table headers/rows `Region`, `Cases`, `North`, `42`. |
| dashboard | smart-gauge-chart | title, min, max, splitNumber | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-051` proves UI click-add, static dataSource edit, representative `visualization.splitNumber` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-gauge-chart`. |
| dashboard | smart-progress | title, target, format, shape | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-052` proves UI click-add, static dataSource edit, representative `visualization.shape` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, default `进度` title, and default size. 2026-06-12 `DWR-001` publishes a real dashboard, opens `/dashboards/view/{code}`, asserts `data-widget-type="smart-progress"` and rendered `75%` computed from static metric data. |
| dashboard | smart-heatmap-chart | title, xField, yField | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-053` proves UI click-add, static dataSource edit, representative `visualization.xField` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-heatmap-chart`. |
| dashboard | smart-treemap-chart | title, nameField, valueField | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-054` proves UI click-add, static dataSource edit, representative `visualization.nameField` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and verifies a non-zero ECharts surface for `smart-treemap-chart`. |
| dashboard | smart-map-chart | title, mapRegion | P1: property panel + render | DONE(saved-payload + runtime): 2026-06-12 `DW-055` proves UI click-add, static dataSource edit, representative `visualization.mapRegion` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-014` opens a published viewer and asserts `smart-map-chart` renders the map placeholder DOM/SVG without unknown-widget fallback. |
| dashboard | smart-leaderboard | title, maxItems, rankField, valueField | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-056` proves UI click-add, static dataSource edit, representative `visualization.maxItems` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-001` opens published viewer runtime, asserts leaderboard registry type, sorted names (`North Zone`, `South Zone`) and formatted metric `9.8K`. |
| dashboard | smart-rich-text | title, content, format | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-057` proves UI click-add, static dataSource edit, representative `visualization.content` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, click-add label title fallback, and default size. 2026-06-12 `DWR-001` opens published viewer runtime and asserts sanitized HTML content renders as visible text. |
| dashboard | smart-image | title, src, alt, objectFit | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-058` proves UI click-add, static dataSource edit, representative `visualization.src` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, click-add label title fallback, and default size. 2026-06-12 `DWR-001` opens published viewer runtime and asserts `img[alt="Runtime SVG Image"]`, `data:image/svg+xml` source and `object-fit: contain`. |
| dashboard | smart-iframe | title, src | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-059` proves UI click-add, static dataSource edit, representative `visualization.src` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, click-add label title fallback, and default size. 2026-06-12 `DWR-001` opens published viewer runtime and asserts sandboxed iframe DOM with `title="Runtime Frame"` and `src="about:blank"`. |
| dashboard | smart-countdown | title, targetDate, format | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-060` proves UI click-add, static dataSource edit, representative `visualization.targetDate` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-001` opens published viewer runtime and asserts countdown title plus custom time-unit labels (`Days`, `Hours`). |
| dashboard | smart-wordcloud-chart | title, shape, colorTheme, gridSize | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-046` proves UI click-add, static dataSource edit, representative `visualization.colorTheme` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-004` opens published viewer runtime, asserts `data-widget-type="smart-wordcloud-chart"`, and verifies a non-zero ECharts root/canvas/SVG surface with no loading/config/error text. |
| dashboard | smart-combo-chart | title, smooth, stack, dataZoom, axes | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-047` proves UI click-add, static dataSource edit, representative `visualization.yAxisLeft.name` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-004` opens published viewer runtime, asserts `data-widget-type="smart-combo-chart"`, and verifies a non-zero ECharts root/canvas/SVG surface with no loading/config/error text. |
| dashboard | smart-nps-chart | title, scoreField, legend, ringWidth | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-048` proves UI click-add, static dataSource edit, representative `visualization.ringWidth` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, default `NPS` title, and default size. 2026-06-12 `DWR-004` opens published viewer runtime, asserts `data-widget-type="smart-nps-chart"`, and verifies a non-zero ECharts root/canvas/SVG surface with no loading/config/error text. |
| dashboard | smart-gallery | title, columns, image/title/description fields | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-049` proves UI click-add, static dataSource edit, representative `visualization.columns` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-004` opens published viewer runtime, asserts `data-widget-type="smart-gallery"`, visible `Runtime Gallery Alpha`, description text, and image alt rendered from authored visualization props. |
| dashboard | smart-kanban | title, group/title/description fields | P1: property panel + render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-050` proves UI click-add, static dataSource edit, representative `visualization.groupField` property edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-004` opens published viewer runtime, asserts `data-widget-type="smart-kanban"`, `Backlog` / `Done` columns, and `Runtime Card A` / `Runtime Card B` cards rendered from static rows. |
| dashboard | smart-stats-row | title | P1: workbench widget render | DONE(saved-payload + representative runtime mount): 2026-06-12 `DW-061` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-001` opens published viewer runtime and asserts shared registry resolves `smart-stats-row`, mounts `stats-row`, and renders four `stat-card-*` elements from the workbench stats hook. Full workbench live-data/interaction semantics remain tracked separately. |
| dashboard | smart-stats-card | title, statKey | P1: workbench widget render | DONE(saved-payload + representative runtime render): 2026-06-12 `DW-062` proves UI click-add, static dataSource edit, representative `visualization.statKey` edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-001` now includes `smart-stats-card`, opens published viewer runtime, asserts shared registry resolves the widget, mounts `stat-card-inbox_pending`, and waits until the card leaves the loading placeholder. |
| dashboard | smart-inbox | title, maxItems, itemTypes | P1: workbench widget render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-063` proves UI click-add, static dataSource edit, representative `visualization.maxItems` edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-007` seeds a pending approval inbox item for the current browser user via `/api/test/fixture` explicit `tenantId/userId`, verifies `/api/inbox` readback, opens published `/dashboards/view/{code}`, waits for the viewer browser `GET /api/inbox`, and asserts the exact inbox row title plus approval type badge render. |
| dashboard | smart-calendar | title | P1: workbench widget render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-064` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-007` reuses the same high-priority pending inbox fixture, opens published `/dashboards/view/{code}`, waits for the viewer browser `GET /api/inbox`, and asserts `smart-calendar` renders a red calendar dot for the API data. |
| dashboard | smart-pipeline | title | P1: workbench widget render | DONE(saved-payload + live CRM runtime + navigation/empty state + variant + permission negative matrix): 2026-06-12 `DW-065` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-008` imports/probes `crm-starter`, seeds account/lead/opportunity/activity through command APIs, verifies `/api/workbench/pipeline` contains the seeded `qualification` amount, opens published `/dashboards/view/{code}`, waits for the viewer browser `GET /api/workbench/pipeline`, asserts `smart-pipeline` renders the stage row, and proves no raw `workbench.pipeline.*` key leaks to the UI. 2026-06-12 `DWR-010` clicks `pipeline-stage-qualification` and asserts canonical `/p/crm_opportunity?filters=...crm_opp_stage=qualification`; `DWR-011` route-fulfills empty stages and asserts `pipeline-crm-unavailable` without raw i18n leakage. 2026-06-12 `DWR-012` route-fulfills deterministic CRM stage variants and asserts all six starter stages render without raw i18n leakage. 2026-06-12 `DWR-013` route-fulfills 403 for `/api/workbench/pipeline`, asserts `pipeline-permission-denied`, and proves it does not collapse into empty data or CRM-unavailable messaging. Product fixes: Workbench pipeline now queries `crm_opp_expected_amount`, aligns stage order with CRM starter, uses `/p/crm_opportunity` for CRM navigation, and distinguishes permission denial from empty/unavailable states. |
| dashboard | smart-leads | title | P1: workbench widget render | DONE(saved-payload + live CRM runtime + navigation/empty state + status variant + permission negative matrix): 2026-06-12 `DW-066` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-008` verifies seeded lead readback through `/api/dynamic/crm_lead/list`, opens published viewer, waits for the viewer browser `GET /api/dynamic/crm_lead/list`, and asserts the unique lead company/contact render in `smart-leads`. 2026-06-12 `DWR-010` clicks the seeded lead row and asserts `/p/crm_lead/view/{recordId}`; `DWR-011` route-fulfills an empty lead list and asserts `leads-empty` without raw i18n leakage. 2026-06-12 `DWR-012` route-fulfills four lead statuses and asserts `lead-status-{status}` badges render. 2026-06-12 `DWR-013` route-fulfills 403 for `/api/dynamic/crm_lead/list`, asserts `leads-permission-denied`, and proves it does not collapse into empty data or CRM-unavailable messaging. Product fixes: widget now uses canonical `/api/dynamic/crm_lead/list`, supports `pid/id/recordId`, exposes stable row/status/permission testids, and navigates to the dynamic detail route. |
| dashboard | smart-activities | title | P1: workbench widget render | DONE(saved-payload + live CRM runtime + navigation/empty state + type variant + permission negative matrix): 2026-06-12 `DW-067` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-008` verifies seeded activity readback through `/api/dynamic/crm_activity/list`, opens published viewer, waits for the viewer browser `GET /api/dynamic/crm_activity/list`, and asserts the unique activity subject renders in `smart-activities`. 2026-06-12 `DWR-010` clicks the seeded activity row and asserts `/p/crm_activity/view/{recordId}`; `DWR-011` route-fulfills an empty activity list and asserts `activities-empty` without raw i18n leakage. 2026-06-12 `DWR-012` route-fulfills four activity types and asserts `activity-type-{type}` icons render. 2026-06-12 `DWR-013` route-fulfills 403 for `/api/dynamic/crm_activity/list`, asserts `activities-permission-denied`, and proves it does not collapse into the empty-state message. Product fixes: widget now uses canonical `/api/dynamic/crm_activity/list`, maps CRM starter `crm_act_*` fields, supports `pid/id/recordId`, exposes stable row/type/permission testids, and falls back to the activity detail route when no related record fields exist. |
| dashboard | smart-my-process | title, maxItems | P1: BPM workbench seam | DONE(saved-payload + live BPM runtime): 2026-06-12 `DW-068` proves UI click-add, static dataSource edit, representative `visualization.maxItems` edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-009` creates/deploys a minimal BPM process, starts a SmartEngine instance, verifies `/api/bpm/workbench`, opens the published viewer, waits for browser `GET /api/bpm/workbench`, asserts the unique businessKey renders, filters to running, and clicks the process row to `/bpm/process-status?processInstanceId={id}`. Product fixes: `getStartedProcesses()` now consumes the canonical workbench envelope and maps SmartEngine `processDefinitionId`/`bizUniqueId` fields; the widget exposes stable row testids and uses the process-status route. |
| dashboard | smart-process-stats | title | P1: BPM workbench seam | DONE(saved-payload + live BPM runtime): 2026-06-12 `DW-069` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-009` verifies `/api/workbench/bpm-stats` contains a running SmartEngine instance, opens the published viewer, waits for browser `GET /api/workbench/bpm-stats`, asserts `process-stats-running-count` is non-zero, and keeps the completion-rate label visible. Product fix: `WorkbenchStatsServiceImpl` now derives BPM running/completed/duration stats from `ab_bpm_audit_record` process_start/process_end audit records instead of the stale execution-log table. |
| dashboard | smart-shortcuts | title, columns | P1: shortcut interaction | DONE(saved-payload + representative runtime interaction): 2026-06-12 `DW-070` proves UI click-add, static dataSource edit, representative `visualization.columns` edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-003` publishes a real dashboard with authored `shortcuts`, opens `/dashboards/view/{code}`, asserts `shortcuts-list`, exact shortcut link `href="/dashboards"`, clicks it, and verifies browser URL navigation. Product fix: `dashboardService.normalizeWidget` now preserves `shortcuts` through the dataSource branch. |
| dashboard | smart-recent | title, maxItems | P1: recent visits data | DONE(saved-payload + representative runtime): 2026-06-12 `DW-071` proves UI click-add, static dataSource edit, representative `visualization.maxItems` edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `UserEngagementControllerIntegrationTest` closes the API contract gap: `POST /api/user-engagement` returns JS-safe string `id`, and immediate `GET engagementType=recent_view&targetType=page` reads back the created page for the same user/tenant. 2026-06-12 `DWR-005` publishes a real dashboard with `smart-recent`, seeds recent data through the engagement API, waits for the viewer's browser `GET /api/user-engagement`, and asserts the recent link title/href renders in `/dashboards/view/{code}`. |
| dashboard | smart-announcement | title | P1: announcement render | DONE(saved-payload + representative runtime): 2026-06-12 `DW-072` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `AnnouncementControllerIntegrationTest` closes the announcement API contract gap: `POST /api/announcements` returns JS-safe string `id` / `publishedBy`, and immediate `GET /api/announcements` reads back the active announcement for the same tenant. 2026-06-12 `DWR-006` publishes a real dashboard with `smart-announcement`, seeds an active pinned urgent announcement through the API, waits for the viewer's browser `GET /api/announcements`, and asserts title/content plus pinned/urgent badges render in `/dashboards/view/{code}`. |
| dashboard | smart-quick-note | title | P1: note persistence | DONE(saved-payload + representative runtime persistence): 2026-06-12 `DW-073` proves UI click-add, static dataSource edit, title edit, Save API, `/api/dashboards/{pid}` readback, exact `type/componentType`, and default size. 2026-06-12 `DWR-002` publishes a real dashboard, opens `/dashboards/view/{code}`, saves note content through `quick-note-textarea`, waits for browser-triggered `PUT /api/user-notes`, asserts saved feedback, reloads, and reads back the persisted note value. |
| report | table | dataSource, columns | P0: save/API/export artifact | DONE: 2026-06-12 `RPT-OP-07` browser click downloads parsable XLSX and `RPT-OP-08` browser click downloads non-empty PDF from saved `extension.reportDsl`; `RPT-OP-11` browser click downloads model/namedQuery/API dataSource XLSX and parses exact sheet rows; backend `ReportExportServiceTest` parses static and resolved dataSource workbooks plus PDF text; BFF binary proxy regression covered |
| report | grouped-table | dataSource, groupByField, columns | P1: save/API/render | DONE: 2026-06-12 static dataSource Excel/PDF semantic artifact; `RPT-OP-09/10` validates group rows and backend unit parses workbook/PDF text |
| report | stat-card | dataSource, valueField, aggregation | P1: save/API/render | DONE: 2026-06-12 static dataSource Excel/PDF semantic artifact; workbook/PDF asserts `Total Cases=24` |
| report | rich-text | content, align | P1: save/API/render | DONE: 2026-06-12 static content Excel/PDF semantic artifact; workbook/PDF asserts paragraph text |
| report | cross-tab | rowField, columnField, valueField | P1: save/API/render | DONE: 2026-06-12 static dataSource Excel/PDF semantic artifact; workbook/PDF asserts pivot columns, row totals, grand total |
| report | chart | chartType, categoryField, valueField | P1: save/API/render | DONE: 2026-06-12 static dataSource Excel/PDF semantic artifact; chart exported as aggregated category/value table, not visual chart image |
| report | barcode | format, staticValue | P1: save/API/render | DONE: 2026-06-12 static text artifact in Excel `Report Text` sheet and PDF content stream |
| report | watermark | text, rotation, opacity | P1: save/API/render | DONE: 2026-06-12 static text artifact in Excel `Report Text` sheet and PDF content stream; visual opacity/rotation not claimed |
| report | page-header | header band | P1: save/API/render | DONE: 2026-06-12 static text artifact in Excel `Report Text` sheet and PDF content stream |
| report | page-footer | footer band | P1: save/API/render | DONE: 2026-06-12 static text artifact in Excel `Report Text` sheet and PDF content stream |

### Phase 2: 旧测试真实性审计

对每个设计器列：

- 当前能作为证据的 spec
- 必须退役的 spec
- 必须修的 spec
- 可暂时标 N/A 的项

任何退役都必须写“替代证据是什么”；没有替代证据则不能退役，只能标 TODO 或产品缺口。

### First Audit Result

Live command:

```bash
rg -n -e 'test\.skip' -e 'test\.fixme' -e 'waitForTimeout\(' -e 'toBeGreaterThanOrEqual\(' -e 'toBeLessThanOrEqual\(' -e 'retries:' -e "waitForEvent\(['\"]download['\"]\)\.catch" -e 'page\.request\.put' -e '__reactProps' \
  web-admin/tests/e2e/page-designer \
  web-admin/tests/e2e/designer \
  web-admin/tests/e2e/bpm-designer \
  web-admin/tests/e2e/automation \
  web-admin/tests/e2e/dashboard \
  -g '*.spec.ts'
```

| 范围 | 审计结论 | 处理 |
|---|---|---|
| Automation Designer | `automation-designer-golden.spec.ts` 已清掉本轮引用路径里的 fixed wait,并用真实 browser/backend runtime fresh run 通过。2026-06-12 新增 `control-delay` 闭环:先用 `AutomationFlowCompilerTest` / `ControlNodeExecutorTest` 红绿验证编译与执行契约,再用 `N-DELAY` 真浏览器拖拽、属性面板、保存回显、webhook 触发、节点状态、下游副作用证明 runtime seam。2026-06-12 新增 `action-start-process` 闭环:先用 direct BPM API 证明 `e2et_payment_approval` 可启动并停在 `manager_review`,再用 `StartProcessActionExecutorTest` 红绿锁定 Automation `CUSTOM` storage 到 BPM `DATABASE` 的 seam,最后用 `N-START-PROCESS` 真浏览器拖拽、process-select、保存回显、enable、webhook fire、execution log、BPM businessKey status 证明 runtime side effect。2026-06-12 新增 `trigger-bpm-event` 闭环:先用 `PropertyFieldRenderer` vitest 红绿锁定静态 multiselect 数组语义,再用 `BpmEventAutomationBridgeTest` 红绿证明 internal EventBusService subscription 和 tenant binding,用 `AutomationTriggerServiceImplTest` 红绿证明 SmartEngine `processKey:version` 匹配裸 processKey,最后用 `N-TRIGGER-BPM-EVENT` 真浏览器拖拽、process-select、eventTypes、保存回显、enable、BPM start、automation log、下游 order item 证明 BPM event consumer seam。2026-06-12 新增 `trigger-inactivity` 闭环:先用 `AutomationSchedulerTest` 红绿锁定调度 delay 可由环境参数覆盖,再用 `AutomationFlowTriggerDeriverTest` / `AutomationFlowConfigDerivationIntegrationTest` 证明 designer flowConfig 派生 inactivity trigger config,用 `FlowPropertyPanel.extensions.test.tsx` 锁定 advanced 分组展开 test seam,最后用 `N-TRIGGER-INACTIVITY` 真浏览器拖拽、advanced 属性组、field/dict picker、保存回显、enable、scheduler sweep、automation log、下游 order item 证明 inactivity consumer seam。`automation-golden.spec.ts` 仍有 loop / llm / start-process skip/fixme。 | 只引用 `automation-designer-golden.spec.ts`;旧 `automation-golden.spec.ts` skip/fixme 不计完成证据。 |
| Page Designer | `designer-deep-operations.spec.ts` 有拖拽、属性编辑、保存发布、撤销重做、预览、删除、导入导出; `unified-designer-kind-and-binding.spec.ts` 与 toolbar permissions 有 seed/环境前置 skip。2026-06-12 新增 `workbench-blocks-runtime.spec.ts`: API 创建/更新/发布 custom list 页后,真实浏览器打开 `/p/c/{pageKey}`,断言 7 个 workbench block mounted、schema readback、static dataSource 渲染、state 联动、附件 href 与 review drawer 浮层。2026-06-12 新增 `standard-blocks-runtime.spec.ts`: API 创建/更新/发布 custom list/form 页后,真实浏览器打开 `/p/c/{pageKey}`,断言剩余 10 个非 workbench block 的 runtime 消费、filter toggle、table data、chart canvas、stat dataSource、selection state、form button navigation;同 spec 追加 table/list runtime action slice,覆盖 keyword search、row action navigation URL 模板、bulk toolbar 与 Bulk Edit modal;2026-06-12 追加 table bulk edit/delete DB side effects,修复 dynamic batch payload 与 DELETE body 契约,并用 API 读回/反查证明持久化和删除只影响目标行。 | 本轮引用 deep/lifecycle/field/smart/list/load-existing fresh run + workbench/standard runtime targeted; seed 前置 skip 不计完成证据。 |
| Dashboard Designer | `dashboard-widget-types.spec.ts` 多处 `>=1` 已于 2026-06-12 收紧:palette count 变为精确 5/14 清单,add-to-canvas 变为精确 count `+1` + `data-widget-type` 画布类型断言。2026-06-12 新增 `WidgetPropertyPanel` 稳定 `widget-prop-*` testid,并用 `DW-037..DW-073` 覆盖当前 dashboard inventory 37 个 widget 的 UI 添加、static dataSource、代表属性编辑、Save API、GET readback,断言 `type/componentType/defaultSize/staticData/edited config`。2026-06-12 新增 `DW-074-01..37`,从 `widgetDefinitions` registry 自动枚举 37 个 widget 的 170 个 `configSchema` 字段,真实浏览器添加 widget 后断言每个 `widget-prop-*` 字段可见,并验证 `dataSource.modelCode/queryCode` 的 aggregate/namedQuery `dependsOn` 分支隐藏/显示。产品侧新增 shared `createWidgetDraft` 工厂,保证 click-to-add 与 drag-to-drop 都保留 registry `defaultConfig`/title fallback。2026-06-12 `dashboard-tab-reorder.spec.ts` 解除 DTR-002/DTR-003 fixme 后 fresh run 通过,覆盖 tab drag、preference write、reload persistence 与 first-time drag hint。2026-06-12 `dashboard-management.spec.ts` 解除 DM-E03/E04/E07/E08/E10/E11/E12 fixme 后 fresh run 通过,覆盖 row action visibleWhen、publish/unpublish/delete lifecycle、row/edit/create navigation、All/Personal/Global tab filter;旧 spec 内 page schema PUT patch 已移除,`onRowClick/detailUrl` 固化到 setup fixture 源头。2026-06-12 新增 `dashboard-widget-runtime.spec.ts` `DWR-001`: API 创建/发布 deterministic dashboard 后,真实浏览器打开 `/dashboards/view/{code}`,断言 `smart-progress` metric percent、`smart-leaderboard` 排序/格式化、`smart-rich-text` HTML 内容、`smart-image` DOM 属性、`smart-iframe` DOM 属性、`smart-countdown` label、`smart-stats-row` workbench registry mount 和 `smart-stats-card` 单卡加载完成。2026-06-12 新增 `DWR-002`: published viewer 中通过 quick-note textarea 真实保存 user note,等待浏览器 `PUT /api/user-notes`,保存反馈可见,reload 后值保持。2026-06-12 新增 `DWR-003`: authored shortcut row survives normalize, renders in published viewer, click navigates to `/dashboards`。2026-06-12 新增 `DWR-007`: current-user inbox fixture 进入 `/api/inbox`,published viewer 中 `smart-inbox` 渲染 exact approval row/type badge,`smart-calendar` 渲染 high-priority red dot;test-only fixture 现在支持显式 `userId` 且 metadata 用 string 保留 snowflake id 精度。2026-06-12 新增 `DWR-008`: `crm-starter` live data 进入 `/api/workbench/pipeline`、`/api/dynamic/crm_lead/list`、`/api/dynamic/crm_activity/list`,published viewer 中 `smart-pipeline` / `smart-leads` / `smart-activities` 渲染 seeded CRM data,且 pipeline 不泄漏 raw `workbench.pipeline.*` key。`dashboard-export.spec.ts` 已覆盖 Excel/PDF artifact。 | 本轮引用 widget/chart/deep/interactions/export fresh run + tab reorder fresh run + management lifecycle fresh run + widget runtime targeted; 37-widget saved-payload matrix 计 representative saved-payload,37-widget `configSchema` matrix 计 property-field presence + `dependsOn` branch visibility。Runtime render path 现在对全 37 个 widget 有 published viewer browser evidence:`DWR-001`/`DWR-004`/`DWR-005`/`DWR-006`/`DWR-007`/`DWR-008`/`DWR-009`/`DWR-010`/`DWR-011`/`DWR-012`/`DWR-013`/`DWR-014`;workbench live-data/interaction semantics 已覆盖 `DWR-002` quick-note persistence、`DWR-003` shortcut navigation、`DWR-005` recent API data、`DWR-006` announcement API data、`DWR-007` inbox/calendar API data、`DWR-008` CRM pipeline/leads/activities API data seam、`DWR-009` BPM my-process/process-stats live data、`DWR-010` CRM canonical navigation、`DWR-011` CRM empty/unavailable states、`DWR-012` CRM variants、`DWR-013` CRM permission states;不外推到每个字段的保存/runtime semantic effect。 |
| BPMN Designer | `web-admin/tests/e2e/bpm-designer/*` 覆盖 userTask/serviceTask/gateway/callActivity/SLA 等 L1/L2/L3;旧 `designer-lifecycle.spec.ts` BPMN 段仍受 permission skip 影响。 | 新 bpm-designer 目录为主要证据;旧 lifecycle skip 不计完成证据。 |
| Report Designer | `report-designer-deep.spec.ts` 的 fixed wait 已替换为 UI/API active wait,并与 `report-designer-smoke.spec.ts` fresh run 通过。2026-06-12 `RPT-OP-07` 从旧的“等待 export API 或 alert”升级为真实 browser download + xlsx parse;产品侧新增 OSS `/api/reports/export/excel`,并修复 PageSchema create extension 丢失与 BFF binary proxy 对 `POST /api/reports/export/excel` 的二进制转发。2026-06-12 `RPT-OP-08` 新增真实 browser download + PDF header/page marker 断言,后端用 saved `extension.reportDsl` 通过 PDFBox 生成 PDF,并由 `PDFTextStripper` 单测解析标题/表头/行。2026-06-12 `RPT-OP-09/10` 新增非 table static artifact: grouped-table 分组、cross-tab pivot、stat 聚合、rich-text 段落、chart 聚合表、barcode/watermark/header/footer 文本 artifact;PDF E2E 解压 Flate content stream 后断言关键文本。2026-06-12 `RPT-OP-11` 新增 model/namedQuery/API dataSource Excel artifact:真实浏览器点击 Export Excel,解析 `Model Orders` / `NamedQuery Orders` / `API Orders` 三个 sheet 的精确行;后端单测覆盖 `DynamicDataService` model filters、`NamedQueryService` params 和 canonical API endpoint delegation。2026-06-12 `RPT-OP-12` 新增 JSON round-trip artifact:真实浏览器点击 Export JSON,解析 `.report.json` 中的 `format/reportPid/reportDsl/dataSets`,后端单测覆盖 saved DSL 与 resolved rows。2026-06-12 `RPT-OP-13` 新增 PDF page-settings/text-hierarchy fidelity:真实浏览器点击 Export PDF,解析 `/MediaBox`、content stream `Tf/Td` 字号与坐标,断言 landscape A4、35mm 左边距、标题 16pt、块标题 12pt 与上下层级;后端 PDFBox 单测先红于固定 `48pt` origin,再由 `page.margin` mm->points 修复转绿。 | Report 10 类 block + 操作 smoke/deep 作为本轮证据;Report table + non-table static Excel/PDF artifact、model/namedQuery/API Excel artifact、JSON round-trip artifact、PDF page-settings/text-hierarchy fidelity 可计入 DONE;PDF chart/barcode/watermark 像素级视觉等价仍未声明。 |

### Live Verification Log

> 2026-06-11 rerun on local real stack. Backend was restarted with `AURA_SSRF_ALLOWED_PRIVATE_HOSTS=127.0.0.1`, `AGENT_LLM_STUB_MODE=true`, `SPRING_PROFILES_ACTIVE=dev`. Local proxy variables were cleared for localhost tests.

| 验证项 | 命令摘要 | 结果 |
|---|---|---|
| Backend health | `curl http://127.0.0.1:6443/actuator/health` with `NO_PROXY=localhost,127.0.0.1` | `{"status":"UP"}` |
| Setup/auth/fixtures | `pnpm exec playwright test -c playwright.config.ts --project=setup --reporter=line` with `IMPORT_TEST_FIXTURES=true` and host plugin root | `16 passed (1.9s)` |
| Typecheck | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| Automation Designer | `tests/e2e/automation/automation-designer-golden.spec.ts` via `playwright.quick.config.ts`, `PW_QUICK_WORKERS=1`, local outbound URLs | `30 passed (2.5m)` |
| 2026-06-12 Backend delay seam | `./gradlew :test --tests com.auraboot.framework.automation.bpm.AutomationFlowCompilerTest --tests com.auraboot.framework.automation.executor.impl.ControlNodeExecutorTest` in `platform` | first run red: `control-delay` unsupported + `duration/unit` ignored; after fix `33 tests completed, 0 failed` |
| 2026-06-12 Setup/auth/fixtures | `pnpm exec playwright test -c playwright.config.ts --project=setup --reporter=line` on host stack `:6443/:5174/:3501` | `16 passed (2.0s)` |
| 2026-06-12 Automation Designer delay targeted | `automation-designer-golden.spec.ts -g "N-DELAY"` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501` | `1 passed (6.4s)` |
| 2026-06-12 Automation Designer full | `tests/e2e/automation/automation-designer-golden.spec.ts` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501` | `31 passed (2.9m)` |
| 2026-06-12 Direct BPM start-process proof | admin login + `POST /api/bpm/process-instances` + `GET /api/bpm/process-instances/by-business-key/status` for `e2et_payment_approval` | process instance created, `manager_review` active, `start_1` completed, variables echoed |
| 2026-06-12 Frontend process picker unit | `pnpm exec vitest run app/shared/services/__tests__/resourceSelectService.test.ts` | `15 tests` passed; new test maps `/api/bpm/process-definitions/deployed` DTO to process-select options |
| 2026-06-12 Backend start-process storage seam | `./gradlew :test --tests com.auraboot.framework.automation.executor.impl.StartProcessActionExecutorTest` in `platform` | first run red: assertion saw outer `CUSTOM` during BPM start; after fix `BUILD SUCCESSFUL`, `7 tests` passed |
| 2026-06-12 Automation Designer start-process targeted | `automation-designer-golden.spec.ts -g "N-START-PROCESS"` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501` | first red: process-select no result due paginated endpoint; second red: webhook 500 from `CustomTaskInstanceStorage.insert()`; third red: list badge stale after toggle; after fixes `1 passed (3.5s)` |
| 2026-06-12 Automation Designer full host-mode | `tests/e2e/automation/automation-designer-golden.spec.ts` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501`, `E2E_OUTBOUND_HOST=127.0.0.1`, `E2E_CALLAPI_OK_URL=http://127.0.0.1:3501/health` | `32 passed (2.0m)` |
| 2026-06-12 Frontend static multiselect unit | `pnpm exec vitest run app/framework/smart/automation/nodes/__tests__/property-panel-render.test.tsx app/framework/smart/automation/nodes/__tests__/triggers.bpm-event.test.ts` | first red: static `multiselect` saved scalar string and backend returned 422 for `eventTypes`; after fix `2 files / 25 tests passed` |
| 2026-06-12 Backend BPM event bridge seam | `./gradlew :test --tests com.auraboot.framework.bpm.listener.BpmEventAutomationBridgeTest` | first red: constructor / `subscribeToBpmEvents()` missing; after fix `6 tests` passed, including EventBusService subscription, tenant context binding, and dispatch failure isolation |
| 2026-06-12 Backend BPM event processKey matching | `./gradlew :test --tests com.auraboot.framework.automation.trigger.impl.AutomationTriggerServiceImplTest --tests com.auraboot.framework.bpm.listener.BpmEventAutomationBridgeTest` | first red: mapper queried `e2et_payment_approval:1`; after fix `34 tests completed, 0 failed` |
| 2026-06-12 Backend restart proof for BPM event bridge | `AURA_SSRF_ALLOWED_PRIVATE_HOSTS=127.0.0.1 AGENT_LLM_STUB_MODE=true SPRING_PROFILES_ACTIVE=dev ./gradlew bootRun --no-daemon`, log `/tmp/aura-decisionops-console-backend-bpmevent-final.log` | health `{"status":"UP"}`; startup log contains subscriptions for `process_started`, `process_ended`, `task_created`, `task_completed`, `task_assigned` |
| 2026-06-12 Setup/auth/fixtures before BPM event E2E | `pnpm exec playwright test -c playwright.config.ts --project=setup --reporter=line` on host stack `:6443/:5174/:3501` | `15 passed, 1 skipped (2.2s)` |
| 2026-06-12 Automation Designer BPM event targeted | `automation-designer-golden.spec.ts -g "N-TRIGGER-BPM-EVENT"` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501` | first red: static multiselect saved scalar string -> HTTP 422; second red: Spring event bridge not invoked; after EventBusService subscription + processKey normalization `1 passed (3.7s)` |
| 2026-06-12 Automation Designer full host-mode after BPM event | `tests/e2e/automation/automation-designer-golden.spec.ts` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501`, `E2E_OUTBOUND_HOST=127.0.0.1`, `E2E_CALLAPI_OK_URL=http://127.0.0.1:3501/health` | `33 passed (2.2m)` |
| 2026-06-12 Backend inactivity scheduler + derivation seam | `./gradlew :test --tests com.auraboot.framework.automation.scheduler.AutomationSchedulerTest --tests com.auraboot.framework.automation.service.AutomationFlowTriggerDeriverTest --tests com.auraboot.framework.automation.AutomationFlowConfigDerivationIntegrationTest` in `platform` | first red: scheduler fixed delay/initial delay hard-coded; after configurable schedule + flowConfig derivation tests `30 tests` passed |
| 2026-06-12 Frontend advanced group + Automation renderer unit | `pnpm exec vitest run app/plugins/core-designer/components/flow-designer-sdk/__tests__/FlowPropertyPanel.extensions.test.tsx app/framework/smart/automation/nodes/__tests__/property-panel-render.test.tsx app/framework/smart/automation/nodes/__tests__/triggers.bpm-event.test.ts` | first red: no stable `prop-group-toggle-advanced`; after `data-testid` + `aria-expanded` + helper expansion `3 files / 29 tests` passed |
| 2026-06-12 Automation Designer inactivity targeted | `automation-designer-golden.spec.ts -g "N-TRIGGER-INACTIVITY"` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501`, `AUTOMATION_INACTIVITY_FIXED_DELAY_MS=5000`, `AUTOMATION_INACTIVITY_E2E_TIMEOUT_MS=90000` | first red: `inactivityField` hidden under collapsed advanced group; second red: test used stale label `订单日期` while fixture metadata label is `下单日期`; after fixes `1 passed (4.6s)` |
| 2026-06-12 Automation Designer full host-mode after inactivity | `tests/e2e/automation/automation-designer-golden.spec.ts` via quick config, `PW_QUICK_WORKERS=1`, host stack `:6443/:5174/:3501`, `E2E_OUTBOUND_HOST=127.0.0.1`, `E2E_CALLAPI_OK_URL=http://127.0.0.1:3501/health`, inactivity scheduler env override | `34 passed (2.4m)` |
| 2026-06-12 Typecheck after inactivity | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| 2026-06-12 Hard redline on Automation referenced spec after inactivity | grep for `test.skip`, `test.fixme`, `waitForTimeout(`, `retries:`, `waitForEvent(download).catch`, `page.request.put`, `__reactProps` on `automation-designer-golden.spec.ts` | no output |
| 2026-06-12 Lower-bound classification after inactivity | grep for `toBeGreaterThanOrEqual` / `toBeLessThanOrEqual` on `automation-designer-golden.spec.ts` | existing `>=1` / `>=2` are business existence lower bounds for side effects/log rows/receiver hits; not used as coverage-completion percentages |
| 2026-06-12 Backend log classification after inactivity | grep backend log for hard failures; generic `ERROR` lines reviewed | no startup failure / unexpected platform hard error; `ERROR` entries are expected sad-path evidence from dangerous expression, upstream 404/500, unknown field, validation, and permission-denied tests |
| 2026-06-12 Diff hygiene after inactivity | `git diff --check` | passed |
| 2026-06-12 Typecheck after BPM event | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| 2026-06-12 Hard redline on Automation referenced spec after BPM event | grep for `test.skip`, `test.fixme`, `waitForTimeout(`, `retries:`, `waitForEvent('download').catch`, `page.request.put`, `__reactProps` on `automation-designer-golden.spec.ts` | no output |
| 2026-06-12 Lower-bound classification after BPM event | grep for `toBeGreaterThanOrEqual` / `toBeLessThanOrEqual` on `automation-designer-golden.spec.ts` | existing `>=1` / `>=2` are business existence lower bounds for side effects/log rows/receiver hits; not used as coverage-completion percentages |
| 2026-06-12 Backend hard-error scan after BPM event | grep backend log for `Application run failed`, `Unexpected system exception`, `BadSqlGrammar`, `DataIntegrityViolation`, `PSQLException`, `Completed 500`, `NullPointerException` | no output |
| 2026-06-12 Diff hygiene after BPM event | `git diff --check` | passed |
| 2026-06-12 Page Designer workbench runtime E2E | `PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BE_PORT=6443 BFF_PORT=3501 PW_WORKERS=1 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/page-designer/workbench-blocks-runtime.spec.ts --project=chromium --no-deps --reporter=line` | `1 passed`; real `/p/c/{pageKey}` browser runtime proof for 7 workbench block types |
| 2026-06-12 Page Designer workbench runtime unit suite | `pnpm exec vitest run packages/core/__tests__/route-manifest.test.ts app/framework/meta/runtime/__tests__/SchemaRuntime.test.ts app/framework/meta/hooks/__tests__/usePageDataSources.test.ts app/framework/meta/rendering/blocks/__tests__/workbench-blocks.test.tsx app/framework/meta/validation/__tests__/block-schema-workbench.test.ts app/shared/services/__tests__/dslRegistryService.test.ts app/plugins/core-designer/components/studio/workbench/designers/areas/__tests__/BlockLibrary.workbench.test.tsx app/plugins/core-designer/components/studio/workbench/designers/areas/__tests__/BlockPreview.workbench.test.tsx` | `8 files / 47 tests passed`; includes `/p/c/:pageKey` route ordering, StrictMode static dataSource rerender, runtime skip-registration boundary |
| 2026-06-12 Page Designer workbench typecheck | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| 2026-06-12 Page Designer workbench e2e-truth redline | grep new spec for `test.skip`, `test.fixme`, `waitForTimeout`, `retries:`, download catch, `__reactProps`, threshold assertions | no output; `page.request.post/put/get` appears only in fixture create/update/publish/readback, not as a replacement for runtime UI assertions |
| 2026-06-12 Backend hard-error scan after Page Designer workbench runtime | grep backend log for `Application run failed`, `Unexpected system exception`, `BadSqlGrammar`, `DataIntegrityViolation`, `PSQLException`, `Completed 500`, `NullPointerException` | no output |
| 2026-06-12 Page Designer standard block runtime unit suite | `pnpm exec vitest run app/ui/schema-renderer/__tests__/BlockRegistry.bootstrap.test.ts app/framework/meta/validation/__tests__/block-schema-workbench.test.ts app/shared/services/__tests__/dslRegistryService.test.ts app/framework/meta/rendering/blocks/__tests__/block-renderer-actions.test.tsx app/framework/meta/rendering/pages/__tests__/DetailPageContent.test.ts` | `5 files / 37 tests passed`; covers 29-type registry bootstrap, fallback validator/registry, `SelectionInfoBlockRenderer`, and `detail-section` page renderer seam |
| 2026-06-12 Page Designer standard block typecheck | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| 2026-06-12 Page Designer standard block runtime E2E | `PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BE_PORT=6443 BFF_PORT=3501 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/page-designer/standard-blocks-runtime.spec.ts --project=chromium --no-deps --reporter=line` | latest rerun after custom block runtime host slice: `8 passed`; real `/p/c/{pageKey}` custom list/form runtime proof for remaining 10 non-workbench block types plus table/list search,row action navigation,bulk toolbar/modal,bulk edit/delete DB side effects,form-buttons create/update command side effects,required/readonly/visibleWhen form-section semantics,custom block alias loader/visibleWhen/error surface,and `e2et_order_form` sub-table create/edit/delete + aggregate readback. |
| 2026-06-12 Page Designer table/list runtime action E2E | same stack, `standard-blocks-runtime.spec.ts --grep "table runtime supports"` | first reds were real oracle/fixture issues: `pageKey` field mismatch (`page_key` is runtime field), non-unique title keyword under parallel fixtures, and brittle split text assertion for bulk selected count; after fixing fixture/oracle, targeted `1 passed` and then-current full standard spec `3 passed` before the later sub-table row raised the spec to `4 passed` |
| 2026-06-12 Page Designer table bulk edit/delete DB side effects | `pnpm exec vitest run app/shared/services/http-client/__tests__/URLBuilder.test.ts app/shared/services/http-client/__tests__/HttpClient.test.ts --reporter=dot`; `pnpm exec tsc --noEmit --pretty false`; `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/page-designer/standard-blocks-runtime.spec.ts --project=chromium --no-deps --reporter=line --grep "table bulk edit and delete"` | TDD red first hit wrong baseURL `5173` enterprise overlay and was reclassified as env归属错误;with `5174` true red was `PUT /api/dynamic/page_schema/batch` 400 because frontend sent `{updates:[...]}` while backend expects array. Fix sends batch update rows with `pid`, sends batch delete as JSON array body, and routes ListPageContent bulk delete through `dynamicService`. Unit `47 passed`; typecheck passed; targeted E2E `1 passed`, proving browser Bulk Edit/Confirm Delete -> batch API -> persisted name readback -> deleted target row absent and host row still present |
| 2026-06-12 Page Designer form-buttons command side effects | `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/page-designer/standard-blocks-runtime.spec.ts --project=chromium --no-deps --reporter=line --grep "form buttons execute"` | first red was test fixture fill strategy: `profile` is rendered as hidden input with default `admin`;after changing helper to fill visible controls and assert hidden defaults, targeted `1 passed`. Evidence covers browser form fill + `Runtime save` click -> `POST /api/meta/commands/execute/pgm:create_page_schema` -> `/api/pages/{pid}` readback, then seeded edit form submit with URL `commandCode=pgm:update_page_schema` -> command response -> backend name update readback. |
| 2026-06-12 Page Designer form field required/readonly/visibleWhen semantics | `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/page-designer/standard-blocks-runtime.spec.ts --project=chromium --no-deps --reporter=line --grep "form fields enforce"` | first red was oracle i18n mismatch: current required validation renders `请填写Runtime name`;after accepting current localized message, targeted `1 passed`. Evidence covers browser submit with required `name` empty, field-level error visible, no `pgm:create_page_schema` request emitted, readonly field value and `readOnly` JS property, and `visibleWhen: ${form.name === "show-extra"}` field becoming visible after real input. |
| 2026-06-12 Page Designer custom block runtime host | `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/page-designer/standard-blocks-runtime.spec.ts --project=chromium --no-deps --reporter=line --grep "custom blocks load"` | first red was oracle overreach: `state` is not part of `/api/pages/key` readback contract. After switching hidden custom block to literal `visibleWhen: ${false}`, targeted `1 passed`. Evidence covers custom-only published list page, backend readback of three custom block configs, alias-based `DecisionFieldImpactBlock` runtime load, hidden block count staying at one, disabled load button with initial current type, and unresolved component surfacing visible `Failed to load` text instead of silent null. |
| 2026-06-12 Backend computedFields partial-update regression | `./gradlew :test --tests com.auraboot.framework.meta.service.CommandExecutorDslEnhancementTest.test21_computedFields_partialUpdateUsesCurrentRecordSnapshot --tests com.auraboot.framework.meta.service.CommandExecutorDslEnhancementTest.test20_computedFields_spelCalculation` in `platform` | first run red: partial update changed qty but field-map test fixture used alias source fields; after correcting fixture and fixing `ComputedFieldsPhase` to merge current record snapshot, `BUILD SUCCESSFUL`, `2 tests` passed |
| 2026-06-12 Page Designer sub-table runtime E2E | same host stack, `standard-blocks-runtime.spec.ts --grep "sub-table runtime"` | first red: obsolete `.sub-table-section` oracle; second red exposed product bug where `e2et:update_order_item` left `e2et_item_subtotal=21` after qty `3->4`; after backend fix/restart, API probe returned `itemSubtotal=28, orderAmount=28` and targeted E2E `1 passed` |
| 2026-06-12 Page Designer runtime combined E2E | `PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BE_PORT=6443 BFF_PORT=3501 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/page-designer/workbench-blocks-runtime.spec.ts tests/e2e/page-designer/standard-blocks-runtime.spec.ts --project=chromium --no-deps --reporter=line` | `5 passed`; proves Page Designer 17 block-type runtime inventory plus table/list search/row-action/bulk-toolbar and sub-table CRUD/aggregate evidence in one targeted browser run |
| 2026-06-12 Page Designer standard block e2e-truth redline | grep workbench + standard runtime specs for `test.skip`, `test.fixme`, `waitForTimeout`, `retries:`, download catch, `__reactProps`, threshold assertions, debug probes, direct `/p` goto classification | no skip/fixme/fixed wait/retry/threshold/debug output; latest standard spec count: 8 tests, 10 `page.request` fixture/readback calls, 39 UI actions, 0 skip/fixme, 0 fixed wait, 0 threshold. `page.request` only fixture create/update/publish/readback and sub-table deterministic API truth readback; direct `/p/c/{pageKey}` / `/p/e2et_order/{pid}/edit` are generated runtime entries, not API bypass |
| 2026-06-12 Inventory sub-table suite triage | `inventory-inbound-subtable.spec.ts` + `inventory-inbound-form-subtable.spec.ts` attempted on host stack | not counted as evidence: current Dev/OSS profile lacks `inv_*`, `pe:*`, `prod_product`, `prod:*`; first failure was `model.prod_product.create` 403, deeper diagnostics showed missing inventory/product models and commands, so this is fixture/profile gap rather than Page Designer sub-table runtime proof |
| 2026-06-12 Page Designer sub-table resolveVia/dataSource modes | `POST /api/plugins/import/import-directory-sync` imported `plugins/showcase` with `autoPublish*` successfully, then `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/showcase/subtable-modes-e2e.spec.ts --project=chromium --no-deps --reporter=line` | first red: `Command not found: sc:create_showcase` because the current host stack had not imported `plugins/showcase`; after importing showcase, targeted E2E `1 passed (3.2s)`. Evidence covers browser sidebar/list/detail navigation and three published-detail schema variants: foreignKey child rows, resolveVia rows, and dataSource rows all render seeded `sc_name/sc_code` values. |
| 2026-06-12 Page Designer layout/container nested tabs | `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/showcase/detail-all-blocks-e2e.spec.ts --project=chromium --no-deps --reporter=line --grep "D.layout-tabs"`; then full file command without grep | first red: schema readback exact-match forgot backend auto-added system `__field_history__` tab; oracle fixed to assert only `layout_*` tabs exactly. Targeted `D.layout-tabs`: `1 passed (4.4s)`. Full file first run failed because the existing suite mutates the same `showcase_all_fields_detail` schema in parallel; describe marked `serial`, final full file `7 passed, 1 skipped (15.4s)`. Evidence covers saved `tabs[].layout` and nested block layout readback, sidebar/list-row detail navigation, `grid-layout` + nested detail-section/rich-text on overview tab, `stack-layout` + divider/rich-text on secondary tab, and active-tab mount/unmount behavior. |
| 2026-06-12 Page Designer runtime health precheck | `curl http://127.0.0.1:6443/actuator/health`; `curl http://127.0.0.1:3501/health`; `curl -I http://127.0.0.1:5174/automation/new` | backend `UP`; BFF healthy and backend `UP`; Vite returns expected login `302` |
| 2026-06-12 Dashboard tab reorder fresh run | `PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BE_PORT=6443 BFF_PORT=3501 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test tests/e2e/dashboard/dashboard-tab-reorder.spec.ts --project=chromium` | after removing DTR-002/DTR-003 fixme: `23 passed, 1 skipped` (setup gated import skip only); covers page load, drag reorder, persisted user preference after reload, and drag hint |
| 2026-06-12 Dashboard tab reorder e2e-truth redline | grep `dashboard-tab-reorder.spec.ts` for `test.skip`, `test.fixme`, `waitForTimeout`, `retries:`, threshold, debug probes, and `page.request.put` classification | no skip/fixme/fixed wait/retry/debug; three `toBeGreaterThanOrEqual(2)` assertions are permanent business preconditions for a draggable tab bar, not coverage thresholds; `page.request.put` only seeds/clears `dashboard_tab_order` preference for deterministic setup, while DTR-003 waits for the browser drag-triggered PUT and verifies reload persistence |
| 2026-06-12 Dashboard management lifecycle targeted | `PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BE_PORT=6443 BFF_PORT=3501 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test tests/e2e/dashboard/dashboard-management.spec.ts --project=chromium -g "DM-E11"` | after enabling lifecycle test and adding per-test cleanup/timeout: `20 passed, 1 skipped` (only setup gated import skip); verifies create fixture -> UI publish -> API status -> UI unpublish -> API status -> UI delete -> row gone |
| 2026-06-12 Dashboard management full fresh run | `PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BE_PORT=6443 BFF_PORT=3501 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test tests/e2e/dashboard/dashboard-management.spec.ts --project=chromium` | after removing DM-E03/E04/E07/E08/E10/E11/DM-E12 fixme and deleting the spec-level page schema PUT patch: `32 passed, 1 skipped`; covers list load/filter, row action visibleWhen, publish/unpublish/delete API responses, row/edit/create navigation to designer, and All/Personal/Global list tabs |
| 2026-06-12 Dashboard management typecheck + e2e-truth redline | `pnpm typecheck`; grep `dashboard-management.spec.ts` for `test.skip`, `test.fixme`, `waitForTimeout`, `retries:`, threshold/debug probes, and `page.request.*` classification | typecheck passed with only existing Vite tsconfig-paths warning; no spec skip/fixme/fixed wait/retry/threshold/debug output; `page.request.post/delete/get/put` is confined to fixture create/cleanup/readback/add-widget and state setup helpers, while lifecycle assertions wait for browser-triggered publish/unpublish/delete responses and UI reload/state changes |
| 2026-06-12 Dashboard registry widget payload typecheck + redline | `pnpm typecheck`; grep `dashboard-widget-types.spec.ts` for `test.skip`, `test.fixme`, `waitForTimeout`, threshold/retry, `page.request.put`, `__reactProps`, debug output | typecheck passed with only existing Vite tsconfig-paths warning; target spec grep has no output. `DW-037..DW-073` now covers 37 widget representative payload cases. |
| 2026-06-12 Dashboard widget payload isolated stack preflight | backend `:6452`, BFF `:3512`, Vite `:5182`; `curl /actuator/health`, `curl /health`, `curl -I /dashboard-designer` | backend `{"status":"UP"}`; BFF healthy with backend `UP`; Vite returned expected auth redirect `302`. Shared `:6443/:5174` was unavailable, so this run used an isolated stack rather than treating shared-stack failure as product failure. |
| 2026-06-12 Dashboard registry widget auth | same isolated stack, `pnpm exec playwright test --project=auth --no-deps` | `4 passed (1.7s)`; admin/operator/viewer storageState regenerated against `:5182`. |
| 2026-06-12 Dashboard registry widget payload targeted E2E | `PW_PROFILE=fast PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5182 BACKEND_URL=http://127.0.0.1:6452 BE_PORT=6452 BFF_PORT=3512 BFF_URL=http://127.0.0.1:3512 VITE_PORT=5182 PW_QUICK_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test -c playwright.quick.config.ts dashboard/dashboard-widget-types.spec.ts --project=chromium -g "Saved Payload"` | first red in this slice: 4 title default mismatches (`smart-progress` default `进度`; content/embed widgets use click-add label fallback), fixed expectations from registry/helper evidence. Final result `37 passed (28.8s)`; `DW-037..DW-073` UI click-add, edit static dataSource and one representative property, click Save, wait for real dashboard save response, read `/api/dashboards/{pid}`, and assert exact type/componentType/default size/staticData/edited config. |
| 2026-06-12 Dashboard widget types full fresh run | same isolated stack, `pnpm exec playwright test -c playwright.quick.config.ts dashboard/dashboard-widget-types.spec.ts --project=chromium` | `73 passed (50.5s)`; palette exact 5/14 label lists, add-to-canvas exact count `+1`, `data-widget-type` exact UI type, property panel labels, and 37 widget saved-payload readbacks all pass. |
| 2026-06-12 Dashboard widget runtime preflight | backend `:6452`, BFF `:3512`, Vite `:5182`; `curl /actuator/health`, `curl /health`, `curl -I /dashboards` | backend `{"status":"UP"}`; BFF healthy with backend `UP`; Vite returned expected unauthenticated login redirect `302` |
| 2026-06-12 Dashboard widget runtime auth | `PW_PROFILE=fast PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5182 BACKEND_URL=http://127.0.0.1:6452 BE_PORT=6452 BFF_PORT=3512 BFF_URL=http://127.0.0.1:3512 VITE_PORT=5182 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test --project=auth --no-deps` | `4 passed (1.6s)`; admin/operator/viewer storageState regenerated against `:5182` |
| 2026-06-12 Dashboard maintained viewer smoke | same isolated stack, `pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/dashboard/dashboard-tab-reorder.spec.ts --project=chromium -g "DTR-001"` | `1 passed (1.4s)`; covers maintained `/dashboards` viewer tab page load. Old `dashboard.spec.ts -g "F3-E01"` against `/reports/overview` failed as pre-existing stale route and is not counted as product evidence for this runtime slice. |
| 2026-06-12 Dashboard widget runtime service unit | `pnpm exec vitest run app/plugins/core-dashboard/services/__tests__/dashboardService.test.ts` | first run red: authored `config.shortcuts` was `undefined` after `normalizeDashboard` dataSource branch; after adding `shortcuts` to passthrough, rerun `5 passed`. |
| 2026-06-12 Dashboard widget runtime targeted E2E | host stack `:6443/:3501/:5174`, `NO_PROXY=localhost,127.0.0.1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BFF_PORT=3501 PW_QUICK_WORKERS=1 pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium` | `DWR-001` first run red because content/embed fixture omitted static `dataSource`, causing legacy normalize to keep `visualization` nested and widgets to show unconfigured states; after aligning fixture to saved-payload contract, rerun passed. `DWR-002` first run red because the saved-feedback assertion only accepted English while the current UI renders `刚刚保存`; after accepting the localized saved label, rerun passed. `DWR-003` first run red because `normalizeDashboard` dropped authored `shortcuts`, so viewer rendered default shortcuts; after product fix, rerun passed. `DWR-004` first run red because the test oracle selected a 16px action-menu SVG instead of the chart surface; after scoping the oracle under `[data-widget-type] .echarts-for-react/canvas/svg`, rerun passed. `DWR-005` first run red because the browser showed stale localStorage recent cache before the async engagement list refresh; after clearing the cache before navigation and waiting for the viewer's `GET /api/user-engagement` response, rerun passed. `DWR-006` adds announcement runtime consumption of active pinned urgent announcement API data. Latest full rerun: `6 passed (7.6s)`. Browser opens `/dashboards/view/{code}` and asserts progress `75%`, leaderboard `9.8K`, rich text content, image/iframe DOM props, countdown labels, stats-row workbench mount, stats-card `inbox_pending` leaving loading placeholder, quick-note UI save -> `PUT /api/user-notes` -> feedback -> reload persistence, shortcut link click navigation to `/dashboards`, advanced widget runtime surfaces/content for wordcloud/combo/nps/gallery/kanban, smart-recent engagement API data rendered as a recent link, and smart-announcement title/content/pinned/urgent DOM rendering. |
| 2026-06-12 Dashboard smart-recent engagement API contract | `./gradlew :test --tests com.auraboot.framework.engagement.UserEngagementControllerIntegrationTest` in `platform` | first red: `POST /api/user-engagement` returned `ASSIGN_ID Long` `id` as JSON number (`idNode.isTextual()` false), while the separate `POST then GET recent_view` contract passed after fixing test cleanup MetaContext. Product fix adds `ToStringSerializer` to `UserEngagementDTO.id` and tightens frontend `UserEngagement.id` / favorite delete / reorder types to `string`. Final rerun: `2 tests` passed, proving JS-safe string ids and read-after-write `recent_view` list membership for the same user/tenant. |
| 2026-06-12 Dashboard smart-recent runtime + audit | `pnpm typecheck`; `pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium -g "DWR-005"`; full runtime spec command above; redline grep on changed spec/service/backend files; `node /Users/ghj/work/auraboot/auraboot-enterprise/scripts/page-golden-audit.mjs <absolute-spec-path>`; `git diff --check` | typecheck passed with only existing Vite tsconfig-paths warning; `DWR-005` targeted `1 passed (1.6s)`; full runtime spec `5 passed (6.7s)`; targeted redline grep had no output; page-golden-audit `PASSED` with `E2E-REQUEST-RATIO` warning (`UI interaction count 2` vs `page.request count 8`) because API is used for dashboard fixture create/publish/cleanup, quick-note original-value restore, and smart-recent engagement fixture create/list/delete, while core evidence remains browser viewer DOM assertions, browser-triggered API responses, chart surface rendering, gallery/kanban content, stats-card loading exit, textarea UI save/reload, shortcut link click navigation, and recent link rendering; diff hygiene passed. |
| 2026-06-12 Dashboard smart-announcement contract + runtime | `./gradlew :test --tests com.auraboot.framework.workbench.AnnouncementControllerIntegrationTest`; `pnpm typecheck`; `pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium -g "DWR-006"`; full runtime spec command above; redline grep on changed announcement/runtime files; `node /Users/ghj/work/auraboot/auraboot-enterprise/scripts/page-golden-audit.mjs <absolute-spec-path>`; `git diff --check` | backend first red: `POST /api/announcements` returned `ASSIGN_ID Long` `id/publishedBy` as JSON numbers, while `POST then GET /api/announcements` active list readback passed. Product fix adds `ToStringSerializer` to `AnnouncementDTO.id` and `publishedBy`, and frontend `AnnouncementItem.id` is now `string`. Final backend rerun: `2 tests` passed. Typecheck passed with only existing Vite tsconfig-paths warning. `DWR-006` targeted `1 passed (7.1s)`; full runtime spec `6 passed (7.6s)`; redline grep had no output; page-golden-audit `PASSED` with `E2E-REQUEST-RATIO` warning (`UI interaction count 2` vs `page.request count 11`) because API is used for dashboard fixture lifecycle plus quick-note/recent/announcement fixture setup/readback/cleanup, while core evidence remains browser viewer DOM and browser-triggered response assertions; diff hygiene passed. |
| 2026-06-12 Dashboard smart-inbox/calendar contract + runtime | `./gradlew :test --tests 'com.auraboot.framework.test.controller.TestFixtureControllerRouteInboxTest'`; BPM smoke; `pnpm typecheck`; `pnpm exec playwright test -c playwright.oss.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium --grep 'DWR-007'`; full runtime spec; e2e-truth redline/counts; `node scripts/validate-plugin-dashboards.mjs`; `git diff --check` | backend first red: new explicit-user fixture test failed because `createInboxItemsWithType` ignored `params.userId`; second red exposed JS precision drift in metadata (`323420212743507968` became `323420212743508000`). Product/test-fixture fix honors explicit `userId` and serializes fixture metadata `tenantId/userId` as strings. Backend targeted fixture class final: `2 tests` passed. BPM smoke before targeted runtime: `20 passed, 1 skipped`. `DWR-007` targeted: `20 passed, 1 skipped`; full dashboard runtime spec: `26 passed, 1 skipped`. Typecheck passed with only existing Vite tsconfig-paths warning. e2e-truth redline: no skip/fixme/fixed wait/threshold/direct `/p/`; only `page.request.put('/api/inbox/{id}/dismiss')` is cleanup. Spec count audit: `tests=7`, `page.request=15`, `ui_assert_or_action=30`; setup remains API-heavy but core evidence is browser `/dashboards/view/{code}`, viewer-triggered `/api/inbox`, exact inbox row/type badge DOM, and calendar red dot DOM. `page-golden-audit.mjs` is absent from this checkout after `find scripts -maxdepth 2`, so it is not claimed; available dashboard static validator `node scripts/validate-plugin-dashboards.mjs` scanned 4 files with `0 failed`. Diff hygiene passed. |
| 2026-06-12 Dashboard CRM workbench contract + runtime | `./gradlew :test --tests com.auraboot.framework.dashboard.service.impl.WorkbenchStatsServiceImplTest --tests com.auraboot.framework.dashboard.service.impl.WorkbenchStatsServiceImplBranchTest`; `pnpm typecheck`; `pnpm exec playwright test -c playwright.oss.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium --grep 'DWR-008'`; full runtime spec; e2e-truth redline/counts; `node scripts/validate-plugin-dashboards.mjs`; `git diff --check` | backend first red: new field-contract test failed because Workbench pipeline SQL still used stale `crm_opp_amount`; product fix uses canonical `crm_opp_expected_amount` for stats/series/pipeline and aligns pipeline stages to CRM starter `discovery/qualification/proposal/negotiation/closed_won`. E2E first product red after fixture calibration: `/api/workbench/pipeline` returned no seeded `qualification` stage; after backend restart with the fix, `DWR-008` targeted passed with setup/auth `20 passed, 1 skipped`. Full dashboard runtime spec: `27 passed, 1 skipped`. Backend targeted classes: `20 tests` passed. Typecheck passed with only existing Vite tsconfig-paths warning. e2e-truth redline: no skip/fixme/fixed wait/retry/React-prop probes; `page.request.post/delete/get` is fixture lifecycle/import/readback, `page.request.put('/api/inbox/{id}/dismiss')` is pre-existing inbox cleanup, and `toBeGreaterThanOrEqual` is a business lower bound for shared CRM data rather than coverage threshold. Static dashboard validator scanned 4 files with `0 failed`; no `page-golden-audit` script exists in this checkout. Diff hygiene passed. |
| 2026-06-12 Dashboard BPM workbench contract + runtime | `pnpm vitest run app/plugins/core-bpm/services/__tests__/bpmWorkbenchService.test.ts`; `./gradlew :test --tests com.auraboot.framework.dashboard.service.impl.WorkbenchStatsServiceImplTest --tests com.auraboot.framework.dashboard.service.impl.WorkbenchStatsServiceImplBranchTest --no-daemon`; `pnpm typecheck`; `pnpm exec playwright test -c playwright.oss.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium --grep 'DWR-009'`; full runtime spec; e2e-truth redline/counts; `git diff --check` | frontend service first red: `getStartedProcesses()` returned the raw `/api/bpm/workbench` envelope instead of normalized started-process rows; product fix maps SmartEngine `processDefinitionId`/`bizUniqueId` to `processDefinitionKey`/`businessKey`. Backend first red: `getBpmStats` still queried stale `ab_bpm_execution_log`; product fix derives running/completed/duration from `ab_bpm_audit_record` process_start/process_end audit records. E2E first stable product red after backend restart: BPM workbench widgets lacked stable runtime testids and `smart-my-process` clicked a stale `/bpm/process/{id}` route; product fix adds widget/row testids and navigates to `/bpm/process-status?processInstanceId={id}`. Final frontend unit: `4 passed`; backend targeted classes: `21 tests` passed; typecheck passed with only existing Vite tsconfig-paths warning. `DWR-009` targeted: `20 passed, 1 skipped`; full dashboard runtime spec: `28 passed, 1 skipped`. |
| 2026-06-12 Dashboard CRM workbench navigation + negative states | `pnpm typecheck`; `pnpm exec playwright test -c playwright.oss.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium --grep 'DWR-010'`; `--grep 'DWR-011'`; full runtime spec; e2e-truth redline/counts; `git diff --check` | E2E first red: `smart-pipeline` clicked stale `/crm_opportunity?filters=...` instead of `/p/crm_opportunity?filters=...`. Second red after route fix: lead/activity list records expose `pid/recordId` rather than `id`, so row-level testids and detail navigation were missing. Product fix canonicalizes CRM workbench navigation to `/p/{model}` / `/p/{model}/view/{recordId}`, supports `pid/id/recordId`, adds stable lead/activity row testids, and lets activity rows fall back to their own detail page when CRM starter has no related-record fields. `DWR-010` targeted: `20 passed, 1 skipped`; `DWR-011` targeted: `20 passed, 1 skipped`; full dashboard runtime spec: `30 passed, 1 skipped`; typecheck passed with only existing Vite tsconfig-paths warning. |
| 2026-06-12 Dashboard widget configSchema field matrix | `pnpm exec tsx -e ...widgetDefinitions...`; RED `npx playwright test -c playwright.config.ts tests/e2e/dashboard/dashboard-widget-types.spec.ts --project=chromium --grep DW-074`; GREEN same command with `--no-deps`; full widget-types spec; `pnpm typecheck`; e2e-truth redline grep; `git diff --check` | Registry extraction: 37 widgets / 170 `configSchema` fields / field types `boolean, icon, model, namedQuery, number, select, text` / 40 dependent fields. RED first run: `55 passed, 1 skipped, 1 failed`, failing only `smart-number-card: icon` because `widget-prop-icon` was missing. Product fix wraps `IconPicker` with schema-keyed `data-testid`. Targeted `DW-074-01..37`: `37 passed`; full `dashboard-widget-types.spec.ts`: `110 passed`. Typecheck passed with only existing Vite tsconfig-paths warning. Redline grep has no skip/fixme/fixed wait/retry/threshold output; only existing `page.request.get('/api/dashboards/{pid}')` readback remains and is backend evidence after UI save, not API-driven authoring. Diff hygiene passed. |
| 2026-06-12 Dashboard CRM workbench stage/status variant matrix | RED/GREEN `npx playwright test -c playwright.oss.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium --grep DWR-012 --no-deps`; full runtime spec; `pnpm typecheck`; e2e-truth redline grep; `git diff --check` | `DWR-012` route-fulfills deterministic variant responses to cover six pipeline stages (`discovery` / `qualification` / `proposal` / `negotiation` / `closed_won` / `closed_lost`), four lead statuses (`new` / `following_up` / `converted` / `disqualified`), and four activity types (`call` / `meeting` / `email` / `note`) in the published viewer. RED first run: `1 failed`, missing `lead-status-new` stable selector. Product fix adds schema-independent runtime testids `lead-status-{status}` and `activity-type-{type}`. Targeted GREEN: `1 passed`; full `dashboard-widget-runtime.spec.ts`: `12 passed`. Typecheck passed with only existing Vite tsconfig-paths warning. Redline grep has no skip/fixme/fixed wait/retry/direct `/p`; existing `page.request.*` remains fixture lifecycle/readback/cleanup, `toBeGreaterThanOrEqual` remains business lower bound, and `page.route` in `DWR-011` / `DWR-012` is deterministic empty/unavailable/variant response injection rather than live-data seam evidence. Diff hygiene passed. |
| 2026-06-12 Dashboard CRM permission negative path + recent pruning stabilization | RED/GREEN `npx playwright test -c playwright.oss.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium --grep DWR-013 --no-deps`; RED/GREEN `./gradlew :test --tests com.auraboot.framework.engagement.service.impl.UserEngagementServiceImplTest --no-daemon`; backend restart + API probe; targeted `--grep "DWR-005\|DWR-013"`; full runtime spec; `pnpm typecheck`; e2e-truth redline grep; `git diff --check` | `DWR-013` route-fulfills deterministic 403 responses for CRM pipeline/leads/activities and asserts `pipeline-permission-denied`, `leads-permission-denied`, and `activities-permission-denied` rather than empty/unavailable states. RED first run: `1 failed`, missing `pipeline-permission-denied`. Product fix adds explicit `permissionDenied` state/testids to all three CRM workbench widgets. Targeted `DWR-013`: `1 passed`; paired `DWR-005|DWR-013`: `2 passed`; full `dashboard-widget-runtime.spec.ts`: `13 passed`. DWR-005 stabilization root cause: API probe showed `POST /api/user-engagement` returned created record with `createdAt`, but immediate list omitted it because historical `createdAt=null` recent rows plus DB ordering let prune delete the new record. Backend RED tests locked timestamp fill and null-history pruning; product fix sets `createdAt/updatedAt` before insert and sorts prune candidates by `updatedAt ?? createdAt` with null timestamps first. Final `UserEngagementServiceImplTest`: 14 tests passed. Typecheck passed with only existing Vite tsconfig-paths warning. Redline grep has no skip/fixme/fixed wait/retry/direct `/p`; `page.request.*` remains fixture lifecycle/readback/cleanup, `toBeGreaterThanOrEqual` remains business lower bound, and `page.route` in `DWR-011` / `DWR-012` / `DWR-013` is deterministic empty/unavailable/variant/permission response injection, complementing `DWR-008` live-data happy path rather than replacing it. Diff hygiene passed. |
| 2026-06-12 Dashboard foundational widget runtime E2E | RED/GREEN `npx playwright test -c playwright.quick.config.ts tests/e2e/dashboard/dashboard-widget-runtime.spec.ts --project=chromium --grep DWR-014 --no-deps`; full runtime spec; `pnpm typecheck`; e2e-truth redline grep; `git diff --check` | `DWR-014` publishes a dashboard with the remaining foundational widget set and opens `/dashboards/view/{code}`. First red was a test oracle strict-mode issue: the map block contained both action-menu SVG and map placeholder SVG; oracle was narrowed to `[data-widget-type="smart-map-chart"] svg`. Targeted GREEN: `1 passed`; full `dashboard-widget-runtime.spec.ts`: `14 passed`; typecheck passed with only existing Vite tsconfig-paths warning. Core evidence: browser DOM asserts `smart-number-card` static KPI `1,234 units`, non-zero ECharts surfaces for bar/line/pie/area/funnel/scatter/radar/gauge/heatmap/treemap, `smart-table-chart` headers/rows, and `smart-map-chart` placeholder DOM/SVG. Redline grep has no skip/fixme/fixed wait/retry/direct `/p`; `page.request.*` remains deterministic fixture lifecycle/readback/cleanup, existing `toBeGreaterThanOrEqual` assertions remain business lower bounds, and existing `page.route` usage is limited to DWR-011/012/013 deterministic empty/unavailable/variant/permission response injection. Diff hygiene passed. |
| 2026-06-12 Report Excel/PDF/backend dataSource unit | `./gradlew :test --tests com.auraboot.framework.bi.ReportExportServiceTest` in `platform` | `6 tests` passed; first red locked non-table gaps, after fix parses generated XLSX and asserts table sheets plus grouped-table/cross-tab/stat/rich-text/chart/report-text sheets; parses generated PDF with `PDFTextStripper` and asserts table + non-table semantic text; model/namedQuery/API dataSource test captures `DynamicQueryRequest` filters and asserts three resolved workbook sheets; missing `extension.reportDsl` throws validation error |
| 2026-06-12 Report Excel BFF binary proxy unit | `pnpm exec vitest run app/server/services/__tests__/BffProxyService.test.ts` | `6 tests` passed; new case covers `/api/reports/export/excel` and `/api/reports/export/pdf` as binary artifact endpoints; proxy now preserves POST method/body for binary routes |
| 2026-06-12 Report Excel typecheck | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| 2026-06-12 Report Excel isolated stack preflight | backend `:6451`, BFF `:3511`, Vite `:5181`; `curl /actuator/health`, `curl /health`, `curl -I /report-designer` | backend `{"status":"UP"}`; BFF healthy with backend `UP`; Vite returned expected auth redirect `302` |
| 2026-06-12 Report Excel targeted E2E | `PW_PROFILE=fast PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5181 BACKEND_URL=http://127.0.0.1:6451 BE_PORT=6451 BFF_PORT=3511 BFF_URL=http://127.0.0.1:3511 VITE_PORT=5181 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test tests/e2e/designer/report-designer-deep.spec.ts --project=chromium-deep --no-deps -g "RPT-OP-07"` | first run red: BFF JSON proxy re-serialized XLSX bytes as `"PK...`; after binary route fix `1 passed (16.7s)` with UI click -> export response -> download filename -> ZIP header -> workbook sheet/header/rows |
| 2026-06-12 Report PDF targeted E2E | same isolated stack, `report-designer-deep.spec.ts --project=chromium-deep --no-deps -g "RPT-OP-08"` | `1 passed (18.1s)` with UI click -> export response -> download filename -> PDF header/page marker |
| 2026-06-12 Report non-table targeted E2E | isolated stack `:6451/:3511/:5181` with Vite `SPRING_BOOT_URL/BFF_INTERNAL_URL=http://127.0.0.1:6451`, `report-designer-deep.spec.ts --project=chromium-deep --no-deps -g "RPT-OP-09\|RPT-OP-10"` | first run environment-invalid: Vite SSR defaulted to backend `:6443` and redirected to login; after restarting Vite with correct backend env `2 passed (31.6s)`. `RPT-OP-09` parses grouped/cross-tab/stat/rich/chart/report-text XLSX sheets; `RPT-OP-10` downloads PDF, verifies header/page marker, and inflates Flate content stream to assert barcode/watermark text |
| 2026-06-12 Report Operations E2E | same isolated stack, `report-designer-deep.spec.ts --project=chromium-deep --no-deps -g "Report Operations"` | `10 passed (2.6m)`; covers operation buttons, save API trigger, export button visibility, table Excel/PDF artifacts, and non-table static Excel/PDF artifacts |
| 2026-06-12 Report model/namedQuery/API preflight | backend `:6452`, BFF `:3512`, Vite `:5182`; `curl /actuator/health`, `curl /health`, `curl -I /report-designer` | backend `{"status":"UP"}`; BFF healthy with backend `UP`; Vite returned expected auth redirect `302` |
| 2026-06-12 Report model/namedQuery/API auth | `PW_PROFILE=fast PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5182 BACKEND_URL=http://127.0.0.1:6452 BE_PORT=6452 BFF_PORT=3512 BFF_URL=http://127.0.0.1:3512 VITE_PORT=5182 PW_WORKERS=1 NO_PROXY=localhost,127.0.0.1 pnpm exec playwright test --project=auth --no-deps` | `4 passed (1.7s)`; admin/operator/viewer storageState regenerated against `:5182` |
| 2026-06-12 Report model/namedQuery/API targeted E2E | same isolated stack, `pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/designer/report-designer-deep.spec.ts --project=chromium -g "RPT-OP-11"` | first run `1 passed (17.4s)`; after removing setup-only `page.request.put` fallback, rerun `1 passed (16.7s)`. Browser clicks Export Excel, waits for export response/download, checks ZIP header, parses workbook sheets `Model Orders` / `NamedQuery Orders` / `API Orders`, and asserts exact model/namedQuery/API rows |
| 2026-06-12 Report Operations E2E with non-static dataSource | same isolated stack, `pnpm exec playwright test -c playwright.quick.config.ts tests/e2e/designer/report-designer-deep.spec.ts --project=chromium -g "Report Operations"` | `11 passed (2.8m)`; covers operation buttons, save API trigger, export button visibility, table Excel/PDF artifacts, non-table static Excel/PDF artifacts, and model/namedQuery/API Excel artifact |
| 2026-06-12 Report model/namedQuery/API typecheck | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| 2026-06-12 Report Excel/PDF e2e-truth redline | grep `report-designer-deep.spec.ts` for `test.skip`, `test.fixme`, `waitForTimeout`, threshold/retry, direct `/p/`, and `page.request.*` classification | no skip/fixme/fixed wait/threshold/retry/direct `/p/` output; `page.request.post('/api/pages')` creates deterministic report fixtures and `page.request.delete` cleans them; core artifact evidence is browser export click + download + XLSX parse/PDF header and inflated content-stream assertions |
| 2026-06-12 Report model/namedQuery/API e2e-truth redline | grep `report-designer-deep.spec.ts` for `test.skip`, `test.fixme`, `waitForTimeout`, threshold/retry, direct `/p/`, `page.request.put`, `waitForEvent('download').catch`, `__reactProps`, `console.log` | no output after removing setup-only `page.request.put`; request ratio remains setup-heavy but core evidence is browser export click + download + parsed XLSX sheet rows, not API export |
| 2026-06-12 Report JSON round-trip backend unit | RED/GREEN `./gradlew :test --tests com.auraboot.framework.bi.ReportExportServiceTest.exportJson_withReportDslAndResolvedRows_rendersRoundTripArtifact --no-daemon`; full class `./gradlew :test --tests com.auraboot.framework.bi.ReportExportServiceTest --no-daemon` | first red: `ReportExportService` had no `exportJson`; after product fix, targeted passed. Full `ReportExportServiceTest`: `7 tests` passed, including JSON artifact format/reportPid/reportDsl/body block/dataSets rows plus existing Excel/PDF contracts. |
| 2026-06-12 Report JSON round-trip targeted E2E | RED/GREEN `NO_PROXY=localhost,127.0.0.1 PW_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 BACKEND_URL=http://127.0.0.1:6443 BE_PORT=6443 BFF_PORT=3501 BFF_URL=http://127.0.0.1:3501 npx playwright test -c playwright.quick.config.ts tests/e2e/designer/report-designer-deep.spec.ts --project=chromium --grep RPT-OP-12 --no-deps --reporter=line` | first red: toolbar had no `Export JSON` button. Product fix adds `/api/reports/export/json`, `reportDesignerService.exportJson`, toolbar button, and `.report.json` download handler. After backend restart, targeted `RPT-OP-12`: `1 passed`; browser clicks Export JSON, waits for POST `/api/reports/export/json` and download, parses JSON, asserts `format`, `reportPid`, saved `reportDsl`, three dataSource declarations, three body titles, and resolved model/namedQuery/API row values. |
| 2026-06-12 Report Operations 07-12 regression + truth | `pnpm typecheck`; backend restart; `report-designer-deep.spec.ts --grep "RPT-OP-0[7-9]\|RPT-OP-1[0-2]" --no-deps`; e2e-truth grep on `report-designer-deep.spec.ts`; `git diff --check` | typecheck passed with only existing Vite tsconfig-paths warning. First 07-12 run failed because the BFF process on `:3501` was stale and still JSON-serialized PDF/XLSX bytes (`"PK...` / `"%PDF...`); source already contained binary route protection, so restarting BFF from current checkout fixed the runtime state. Final 07-12 regression: `6 passed`. Truth grep has no skip/fixme/fixed wait/retry/threshold/direct `/p`; `page.request.post/get/delete` is deterministic report/namedQuery fixture setup/readback/cleanup, while core evidence remains browser export clicks + downloaded artifact parses. Diff hygiene passed. |
| 2026-06-12 Report PDF visual-fidelity backend unit | RED/GREEN `./gradlew :test --tests com.auraboot.framework.bi.ReportExportServiceTest.exportPdf_withPageSettings_preservesMediaBoxMarginsAndTextHierarchy --no-daemon`; full class `./gradlew :test --tests com.auraboot.framework.bi.ReportExportServiceTest --no-daemon` | first red: PDF title x-coordinate was fixed `48pt` instead of DSL `35mm` margin (`98.2pt`). Product fix converts `page.margin` mm to PDF points and applies top/right/bottom/left margins to content origin, page-break threshold, and wrap width while preserving legacy `48pt` default when no margin exists. Targeted passed; full `ReportExportServiceTest`: `8 tests` passed. |
| 2026-06-12 Report PDF visual-fidelity E2E | RED/GREEN `report-designer-deep.spec.ts --grep RPT-OP-13 --no-deps`; regression `--grep "RPT-OP-0[7-9]\|RPT-OP-1[0-3]"`; full `--grep "Report Operations"`; e2e-truth grep | first red: browser-downloaded PDF content stream had title x `48` instead of expected 35mm left margin. After backend restart with product fix, `RPT-OP-13`: `1 passed`; 07-13 regression: `7 passed`; full Report Operations: `13 passed`. The E2E parses `/MediaBox`, content stream `Tf` font sizes, and `Td` text coordinates to assert landscape A4, 35mm left margin, title font size 16, block title font size 12, and vertical hierarchy. Truth grep has no skip/fixme/fixed wait/retry/threshold/direct `/p`; `page.request.*` remains deterministic fixture setup/readback/cleanup. |
| 2026-06-12 Automation full first run classification | same spec without host-mode outbound override | environment-invalid for local host stack: default outbound URL targeted `host.docker.internal:6444`; rerun above is the product evidence |
| 2026-06-12 Typecheck | `pnpm typecheck` in `web-admin` | passed; only existing Vite tsconfig-paths warning |
| 2026-06-12 Hard redline on Automation referenced spec | grep for `test.skip`, `test.fixme`, `waitForTimeout(`, `retries:`, `waitForEvent('download').catch`, `page.request.put`, `__reactProps` on `automation-designer-golden.spec.ts` | no output |
| 2026-06-12 Lower-bound classification | grep for `toBeGreaterThanOrEqual` / `toBeLessThanOrEqual` on `automation-designer-golden.spec.ts` | existing `>=1` / `>=2` are business existence lower bounds for side effects/log rows/receiver hits; not used as coverage-completion percentages |
| 2026-06-12 Diff hygiene | `git diff --check` | passed |
| BPMN Designer | `tests/e2e/bpm-designer` via quick config | `21 passed (48.8s)` |
| Page Designer | `designer-deep-operations`, `page-designer-full-lifecycle`, `field-properties`, `smart-components`, `list-config-layout`, `load-existing-page` via quick config | `34 passed (1.1m)` |
| Dashboard Designer | `dashboard-export`, `dashboard-designer-deep`, `dashboard-widget-types`, `dashboard-charts`, `dashboard-interactions` via quick config | `64 passed (56.2s)` |
| Report Designer | `report-designer-smoke`, `designer/report-designer-deep` via quick config | `98 passed (19.9m)` |
| Hard redline on referenced specs | grep for `test.skip`, `test.fixme`, `waitForTimeout(`, `retries:`, `waitForEvent('download').catch`, `page.request.put`, `__reactProps` on only the fresh-run evidence specs above | no output |
| Full redline inventory | same grep across all designer-related specs | still lists old/unreferenced suites and lower-bound assertions; see classification below |
| Diff hygiene | `git diff --check` | passed |

### Evidence Status Snapshot

| 范围 | 本轮可计入 DONE 的证据 | 明确不能计入 DONE 的剩余项 |
|---|---|---|
| Automation Designer | record-create/update/field-change/state-change/scheduled/webhook/bpm-event/inactivity triggers; update/create/notification/execute-command/call-api/send-webhook/start-process/llm-call actions; condition/loop/delay controls; true/false/edge/lifecycle/concurrency/i18n sad/edge/corner paths. Evidence: `automation-designer-golden.spec.ts` 34 pass with real side effects/logs/outbound receiver/scheduler sweep, including `N-DELAY` for `control-delay` UI save + SmartEngine runtime + downstream side effect, `N-START-PROCESS` for process-select + BPM business process start + `manager_review` wait-state correlation, `N-TRIGGER-BPM-EVENT` for BPM `task_assigned` event -> automation log -> downstream order item, and `N-TRIGGER-INACTIVITY` for stale-date/state filters -> scheduler sweep -> automation log -> downstream order item. Backend seam evidence: `AutomationFlowCompilerTest` + `ControlNodeExecutorTest` targeted pass for delay, `StartProcessActionExecutorTest` targeted pass for storage-mode boundary, `BpmEventAutomationBridgeTest` targeted pass for internal subscriber + tenant context, `AutomationTriggerServiceImplTest` targeted pass for versioned processKey matching, `AutomationSchedulerTest` + `AutomationFlowTriggerDeriverTest` + `AutomationFlowConfigDerivationIntegrationTest` targeted pass for inactivity schedule/config derivation. | Legacy `automation-golden.spec.ts` skip/fixme is not evidence. |
| BPMN Designer | Base palette 9 types are covered through L1 designerJson, L2 BPMN XML, and selected L3 runtime: start/end, userTask assignee/form/MI/SLA, serviceTask command/http/rule/notification, receiveTask, exclusive/parallel/inclusive gateway, callActivity. Evidence: `tests/e2e/bpm-designer` 21 pass. | Old `designer-lifecycle.spec.ts` BPMN permission skip and old gateway lifecycle skip are not evidence. |
| Page Designer | Form-page designer operations: drag/sort, property edit, save/publish, undo/redo, preview, field drag, delete, outline, multi-type mix, PageSchema V2 import/export, lifecycle, field/smart component panels, list layout/load-existing/error state. Evidence: targeted Page Designer 34 pass. 2026-06-12 added `workbench-blocks-runtime.spec.ts` for the 7 workbench block types: published custom list page, `/p/c/{pageKey}` runtime load, schema readback, static dataSource rendering, state/context propagation, action-bar visibility transitions, attachment href, and review drawer floating UI. 2026-06-12 added `standard-blocks-runtime.spec.ts` for the remaining 10 non-workbench block types: published custom list/form pages, filter toggle, toolbar, table data, text, stat dataSource, chart canvas, detail-section, selection state, form fields, form button navigation, table search/row-action/bulk toolbar, table bulk edit/delete persisted DB side effects, form-buttons create/update command side effects, form-section required/readonly/visibleWhen runtime semantics, custom block runtime host/loader/error surface, and real `e2et_order_form` sub-table child create/edit/delete with backend aggregate readback. 2026-06-12 added `showcase/subtable-modes-e2e.spec.ts`, proving sub-table foreignKey / resolveVia / dataSource modes by importing `plugins/showcase`, seeding `showcase_all_fields`, swapping `showcase_all_fields_detail` blocks, and asserting browser-rendered rows/seeded values for all three variants. 2026-06-12 added `showcase/detail-all-blocks-e2e.spec.ts` `D.layout-tabs`, proving detail runtime nested tabs with persisted grid/stack layout schema, nested block layout readback, tab switching, and active-tab child block mount/unmount. | Page Designer 17 block-type runtime inventory rows are DONE, table/list bulk edit/delete DB side effects are DONE, form-buttons create/update command side effects are DONE, generic form-section field semantics are PARTIAL-DONE for required/readonly/visibleWhen, custom block runtime host is PARTIAL-DONE for alias loader/visibleWhen/error surface, sub-table CRUD/aggregate plus foreignKey/resolveVia/dataSource mode rows now have runtime/backend evidence, and layout/container runtime nesting is PARTIAL-DONE for detail tabs/grid/stack. Full Page Designer cartesian coverage is still not complete: component-family depth such as layout/container designer authoring nesting/move/delete/undo/property combinations, upload/download artifact, text/number/reference/select/date type variants, custom block props schema/valueField/property-panel save semantics, and all property-panel combinations remain tracked by the higher-level Page Designer matrix rows. |
| Dashboard Designer | Dashboard designer load, chart palette/config, widget type add/property-panel checks, data source binding, resize/settings/validation/publish/unpublish/layout, interactions, Excel XLSX artifact parse, PDF header/content marker. Evidence: Dashboard 64 pass from the broader suite, plus `dashboard-widget-types.spec.ts` `73 passed` in the isolated widget payload fresh run. 2026-06-12 added `dashboard-tab-reorder.spec.ts` fresh run with DTR-002/DTR-003 enabled, covering drag reorder and user-preference reload persistence. 2026-06-12 added `dashboard-management.spec.ts` fresh run with DM-E03/E04/E07/E08/E10/E11/DM-E12 enabled, covering row action visibleWhen, publish/unpublish/delete lifecycle, row/edit/create navigation, and All/Personal/Global tab filtering; spec-level page schema PUT patch removed and `onRowClick/detailUrl` lives in setup fixture source. 2026-06-12 added widget payload precision: `dashboard-widget-types.spec.ts` no longer uses `>=1` for add/count; every add assertion checks exact count `+1` and exact `data-widget-type`; `DW-037..DW-073` prove 37 widget UI add -> static dataSource edit -> representative property edit -> save -> backend readback preserves exact registry type/componentType/default size/staticData/edited config. 2026-06-12 added `DW-074-01..37`, proving registry-driven property-panel field presence for 37 widgets / 170 `configSchema` fields, including aggregate/namedQuery `dependsOn` branch visibility. 2026-06-12 added `dashboard-widget-runtime.spec.ts` `DWR-001`, proving published Dashboard Viewer runtime semantics for representative display/data/embed/workbench widgets: progress computed percent, leaderboard sorted/formatted rows, rich-text sanitized content, image/iframe DOM props, countdown labels, stats-row workbench mount, and stats-card single-card statKey render. 2026-06-12 added `DWR-002`, proving `smart-quick-note` viewer persistence through real textarea input, browser-triggered `PUT /api/user-notes`, localized saved feedback, and reload readback. 2026-06-12 added `DWR-003`, proving `smart-shortcuts` authored rows survive normalize, render in viewer, and navigate on click. 2026-06-12 added `DWR-004`, proving representative advanced widget runtime rendering for wordcloud/combo/nps ECharts surfaces, gallery authored DOM/image content, and kanban grouped columns/cards. 2026-06-12 added `DWR-005` plus `UserEngagementControllerIntegrationTest`, proving `smart-recent` consumes engagement API recent-page data in the viewer and engagement ids are JS-safe strings; later DWR-005 stabilization fixed recent_view pruning against historical null timestamps. 2026-06-12 added `DWR-006` plus `AnnouncementControllerIntegrationTest`, proving `smart-announcement` consumes active pinned urgent announcement API data in the viewer and announcement `id/publishedBy` are JS-safe strings. 2026-06-12 added `DWR-007` plus `TestFixtureControllerRouteInboxTest`, proving current-user pending approval inbox fixture is read back through `/api/inbox`, `smart-inbox` renders the exact row/type badge in the viewer, and `smart-calendar` renders the high-priority red dot. 2026-06-12 added `DWR-008` plus `WorkbenchStatsServiceImplTest`, proving CRM workbench widgets consume live CRM runtime data: `smart-pipeline` reads `/api/workbench/pipeline` with canonical `crm_opp_expected_amount`, `smart-leads` reads `/api/dynamic/crm_lead/list`, and `smart-activities` reads `/api/dynamic/crm_activity/list` / `crm_act_*` fields. 2026-06-12 added `DWR-010` / `DWR-011`, proving CRM workbench widget clicks navigate to canonical dynamic pages and render empty/unavailable states without raw i18n leakage. 2026-06-12 added `DWR-012`, proving CRM workbench frontend variant rendering for six pipeline stages, four lead statuses, and four activity types via deterministic route fulfillment. 2026-06-12 added `DWR-013`, proving CRM workbench 403/permission-denied responses render explicit permission states and do not collapse into empty/unavailable messaging. 2026-06-12 added `DWR-009`, proving BPM workbench widgets consume live SmartEngine runtime data: `smart-my-process` reads `/api/bpm/workbench`, renders the started instance, filters running processes, and navigates to process-status; `smart-process-stats` reads `/api/workbench/bpm-stats` and renders a non-zero running count from audit records. 2026-06-12 added `DWR-014`, proving the remaining foundational widgets render in published viewer runtime: number-card value, bar/line/pie/area/funnel/scatter/radar/gauge/heatmap/treemap ECharts surfaces, table headers/rows, and map placeholder DOM/SVG. Product fixes: click-to-add and drag-to-drop now share `createWidgetDraft`, preserving registry `defaultConfig` and label fallback for empty titles; `WidgetPropertyPanel` exposes `widget-prop-*` testids for property edit evidence, including icon picker fields; `dashboardService.normalizeWidget` preserves `shortcuts` through the dataSource branch; `UserEngagementDTO.id` and `AnnouncementDTO.id/publishedBy` now serialize ASSIGN_ID Long ids as strings; recent_view upsert now assigns timestamps before prune and prunes null-timestamp history before new records; inbox test fixture supports explicit `userId` and serializes fixture `tenantId/userId` metadata as strings; CRM workbench endpoints/fields now align to `crm-starter` config, pipeline stage labels no longer leak raw keys, CRM widget navigation uses canonical `/p` dynamic routes, lead/activity badges expose stable status/type testids, and CRM permission-denied states are explicit; BPM workbench service maps SmartEngine fields and BPM stats now read `ab_bpm_audit_record`. | 37-widget representative saved-payload matrix is closed. 37-widget `configSchema` field presence + `dependsOn` branch visibility matrix is closed. Runtime render paths now have browser evidence for all 37 widgets (`DWR-001` + `DWR-004` + `DWR-005` + `DWR-006` + `DWR-007` + `DWR-008` + `DWR-009` + `DWR-010` + `DWR-011` + `DWR-012` + `DWR-013` + `DWR-014`); workbench live-data/interaction semantics is additionally closed for quick-note persistence, shortcut navigation, recent-visit data rendering, active announcement rendering, inbox/calendar API data rendering, CRM pipeline/leads/activities live-data + navigation + empty states + stage/status/type frontend variants + permission negative states, and BPM my-process/process-stats live-data rendering. This does not claim every config field's save/runtime semantic effect or richer Report PDF chart/barcode/watermark visual equivalence. |
| Report Designer | 10 report block types (`data-table`, `grouped-table`, `stat-card`, `rich-text`, `cross-tab`, `chart`, `barcode`, `watermark`, `page-header`, `page-footer`) plus save/export operation smoke/deep. Evidence: Report 98 pass after fixed-wait removal. 2026-06-12 added Report table Excel artifact closure: `ReportExportServiceTest` backend workbook parse, `BffProxyService.test.ts` binary proxy regression, `RPT-OP-07` browser export click + download filename + ZIP header + `xlsx` sheet/header/rows parse. 2026-06-12 added Report table PDF artifact closure: OSS `/api/reports/export/pdf`, backend PDFBox renderer + `PDFTextStripper` parse, `RPT-OP-08` browser export click + download filename + PDF header/page marker. 2026-06-12 added Report non-table static artifact closure: backend workbook/PDF semantic parse for grouped-table/cross-tab/stat/rich-text/chart/barcode/watermark/header/footer, `RPT-OP-09` browser export click + multi-sheet XLSX parse, `RPT-OP-10` browser export click + PDF header/page marker + inflated content-stream text assertions, and Report Operations 10-pass run. 2026-06-12 added Report model/namedQuery/API Excel dataSource artifact closure: backend unit resolves `model` via `DynamicDataService`, `namedQuery` via `NamedQueryService`, `api` via canonical dataSource endpoint delegation; `RPT-OP-11` browser export click downloads and parses three exact sheets/rows; that slice reached `11 passed`. 2026-06-12 added Report JSON round-trip artifact closure: backend emits `auraboot.report.export.v1` with saved `reportDsl` and resolved `dataSets`; `RPT-OP-12` browser export click downloads `.report.json` and parses model/namedQuery/API rows; Report Operations 07-12 regression `6 passed` after restarting stale BFF runtime. 2026-06-12 added Report PDF page-settings/text-hierarchy fidelity closure: backend unit first red on fixed 48pt origin then green after `page.margin` mm->points conversion; `RPT-OP-13` browser export click downloads PDF and parses `/MediaBox` plus inflated content stream to verify landscape A4, left margin, title/block-heading font sizes, and vertical hierarchy; Report Operations 07-13 regression `7 passed`, full Report Operations `13 passed`. Product fixes: saved full Report DSL in `extension.reportDsl`, preserved PageSchema create extension, binary-proxied `POST /api/reports/export/excel|pdf`, added `POST /api/reports/export/json`, replaced the dangling `/api/print/render-html` frontend PDF call with saved-report export, added semantic export projections for non-table static blocks, resolved non-static dataSources for export without absolute API fetch, added frontend Export JSON download wiring, and applied DSL page settings/margins to PDF layout coordinates/wrap width. | Report table + non-table static Excel/PDF artifacts, model/namedQuery/API Excel artifact, JSON round-trip artifact, and PDF page-settings/text-hierarchy fidelity are DONE. Do not claim Report export all-formats/all-dataSources/richer chart-barcode-watermark visual equivalence completion. |

### Truth Review Notes

- 本轮声明只能说“上述 fresh-run evidence specs 通过且 hard redline clean”。不能说“全量设计器笛卡尔积 100% DONE”。
- Full redline 仍列出旧套件的 `test.skip` / `test.fixme` / API PUT setup / lower-bound assertions;这些文件不作为本轮 DONE 证据。
- `toBeGreaterThanOrEqual` 在本轮引用 spec 中只作为业务下限使用,不能用于计算覆盖率或证明某一完整组件族已全部覆盖。`dashboard-widget-types.spec.ts` 的 add/count 阈值已移除,由精确 `+1` / 精确 `data-widget-type` / 精确 5/14 清单和 `DW-037..DW-073` saved-payload 证据替代。
- Page Designer custom block DONE 边界只到 custom-only list host、registered runtime component loader、block-level `visibleWhen` 和 unresolved component visible error;不能外推为每个 typed custom block 的 props schema/valueField/property-panel 保存语义。
- 2026-06-12 `control-delay` 的 DONE 只覆盖设计器配置 `duration/unit` → 后端编译/执行 → SmartEngine 节点状态 → 下游副作用闭环。
- 2026-06-12 `action-start-process` 的 DONE 只覆盖设计器配置 `processKey/businessKey/variables` → 保存回显 → Automation webhook runtime → BPM durable process start → by-businessKey status 闭环。
- 2026-06-12 `trigger-bpm-event` 的 DONE 只覆盖设计器配置 `processKey/eventTypes` → 保存回显 → EventBusService internal subscriber → AutomationTriggerService BPM matching → SmartEngine automation runtime → 下游副作用闭环。
- 2026-06-12 `trigger-inactivity` 的 DONE 只覆盖设计器配置 `modelCode/inactivityHours/inactivityField/stateField/inactivityStates` → 保存回显 → configurable scheduler sweep → AutomationTriggerService inactivity matching → SmartEngine automation runtime → 下游副作用闭环。
- 2026-06-12 Page Designer workbench DONE 单独只覆盖 schema-level static dataSource + `/p/c/{pageKey}` runtime for 7 workbench block types,以及 state/context/dataSource 三路联动;它需要和后续 `standard-blocks-runtime.spec.ts` 合并后,才构成 17 个 block-type runtime inventory DONE。
- `workbench-blocks-runtime.spec.ts` 中的 `page.request.post/put/get` 只用于 fixture create/update/publish/readback,因为本轮验证对象是 consumer runtime,不是设计器 UI 建页路径;运行页断言仍由真实浏览器打开 `/p/c/{pageKey}` 完成,不作为 PUT API 绕过 runtime 证据。
- 本轮 Page Designer 产品 seam 修复包括:新增 `/p/c/:pageKey` route manifest 顺序保护,避免 custom page 落入菜单 catch-all;修复 `usePageDataSources` 在 React StrictMode cleanup 后的重新注册与 static dataSource rerender;新增 `SchemaRuntime.skipDataSourceRegistration` 边界,由 page-level manager 统一注册 schema dataSources。
- 2026-06-12 Page Designer standard DONE 只覆盖剩余 10 个非 workbench block 的 published-schema → runtime consumer seam: `filters/form-section/form-buttons/table/detail-section/text/toolbar/selection-info/stat-card/chart-card`。2026-06-12 table/list action slice 进一步覆盖 custom runtime keyword search、row action navigation URL 模板、row checkbox selection、bulk toolbar 与 Bulk Edit modal;2026-06-12 bulk side-effect slice 覆盖 Bulk Edit/Confirm Delete browser 操作、dynamic batch PUT/DELETE、API 读回 name 变更、目标行删除反查与 host row 保留;2026-06-12 form command slice 覆盖 custom form create command 与 seeded edit form update command 的浏览器提交、command response、`/api/pages/{pid}` 后端读回;2026-06-12 form field semantic slice 覆盖空 required submit 拦截、readonly 默认值与 visibleWhen 表单值联动。它不等于 Page Designer form/list 族所有属性组合完成。
- 2026-06-12 table/list bulk edit/delete 首次红灯先暴露运行环境归属问题:quick config 默认 `localhost:5173` 命中企业 overlay staging,而目标 worktree Vite 在 `5174`;所有 Page Designer quick run 必须显式 `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174`。真实红灯为 frontend batch payload shape 错误:`BulkEditModal`/`dynamicService.batchUpdate` 发送 `{updates:[{id,data}]}`,后端 `DynamicController.batchUpdate` 期望 `List<Map>`;bulk delete 旧路径把 ids 放进 DELETE query,后端期望 JSON array body。修复后 `URLBuilder` 仅对 array DELETE 使用 JSON body,object DELETE 仍保持 query 兼容。
- 2026-06-12 Page Designer sub-table CRUD/aggregate DONE 覆盖已发布 `e2et_order_form` 的真实运行页:浏览器打开 `/p/e2et_order/{pid}/edit`,通过 `subtable-empty-action` 新增子行,调用 `e2et:create_order_item`,UI 和 API 断言 `e2et_item_subtotal=21`、父单 `e2et_order_amount=21`;随后 inline edit qty `3->4`,调用 `e2et:update_order_item`,UI/API 断言 `subtotal=28`、父单 amount/qty 更新;最后删除子行并反查子表为空。它不声明 Page Designer palette 当前暴露了 sub-table block;三种数据模式由 `showcase/subtable-modes-e2e.spec.ts` 单独计入。
- 2026-06-12 sub-table 首次红灯暴露后端真实缺口:`ComputedFieldsPhase` 在 update 阶段只用 payload/fieldMapResults 计算,未合并当前记录快照,所以部分字段 update 后 `e2et_item_qty` 变为 4 但 `e2et_item_subtotal` 仍为 21,aggregate 也只能汇总旧 subtotal。产品修复为 computedFields 计算前读取 field-map 后的当前记录快照,再让 payload 覆盖;后端新增 `test21_computedFields_partialUpdateUsesCurrentRecordSnapshot` 红绿锁定。
- 2026-06-12 `inventory-inbound-subtable.spec.ts` / `inventory-inbound-form-subtable.spec.ts` 不计入本轮证据。当前 host Dev/OSS profile 下 `inv_warehouse`、`inv_inbound`、`inv_inbound_line`、`prod_product` model meta 和 `inv:*`、`pe:*`、`prod:*` commands 缺失,首个 `prod_product` 403 后继续诊断为 fixture/profile gap;不能把该套件失败归为 Page Designer sub-table runtime 产品失败,也不能把它当 sub-table 完成证据。
- 2026-06-12 `showcase/subtable-modes-e2e.spec.ts` 覆盖 sub-table foreignKey / resolveVia / dataSource 三种数据模式:setup 先导入 `plugins/showcase`,用 `sc:create_showcase` 创建 deterministic parent/child data,并用 `PUT /api/pages/{pid}` 临时替换/恢复 `showcase_all_fields_detail` blocks;这些 `page.request` 是 seed/schema restore,不是 UI 替代证据。核心证据来自浏览器侧边栏/list/detail 导航后分别断言子表标题、行数据和 seeded `sc_name/sc_code`。真实性计数:`1` test,`4` pageRequest,`5` UI actions,`0` skip/fixme/fixed wait/threshold。
- 2026-06-12 `showcase/detail-all-blocks-e2e.spec.ts` 新增 `D.layout-tabs` 覆盖 layout/container runtime nesting:setup 用 `PUT /api/pages/{pid}` 临时替换/恢复 detail schema,并通过 `/api/pages/key/showcase_all_fields_detail` 读回 `layout_*` tabs 的 `grid/stack` layout 与 nested block `layout.order/colSpan`;核心证据来自浏览器侧边栏/list-row detail 导航后断言 overview tab 的 `grid-layout`、`grid-item-*`、nested detail-section/rich-text,secondary tab 的 `stack-layout`、divider/rich-text,以及 active tab mount/unmount。完整文件必须 serial,因为所有用例共享并恢复同一份 `showcase_all_fields_detail` schema;最终 `7 passed,1 skipped`,skip 是既有"detail-section + sub-table 已由其它 spec 覆盖"设计性 skip。真实性计数:`8` tests,`4` pageRequest,`13` UI actions,`1` design skip,`0` fixed wait,`1` threshold;threshold 是 monthly-grid `>=27` 单元格业务下限,不是覆盖率阈值。
- `standard-blocks-runtime.spec.ts` 中的 `page.request.post/put/get` 同样只用于 fixture create/update/publish/readback 与 command side-effect 后端读回;真实证据是浏览器打开 `/p/c/{pageKey}` / seeded edit form 后的 filter toggle、keyword search、table data、row action click/navigation、bulk toolbar/modal、chart canvas、stat value、state mutation、form field、form button navigation、required validation block、readonly/visibleWhen runtime state、form submit command response 和 API 持久化断言。
- `standard-blocks-runtime.spec.ts` 的 direct `page.goto('/p/c/{pageKey}')` 是动态发布临时 custom page 的唯一 runtime entry,不是 smoke spec 直达已有菜单页,也不是 API 替代 UI 消费路径。
- 2026-06-12 Dashboard tab reorder DONE 只覆盖 dashboard viewer tab bar: page load, drag reorder, preference persistence after reload, and drag hint. `page.request.put('/api/user-preferences/dashboard_tab_order')` 只用于 deterministic setup/cleanup;核心持久化证据来自浏览器 drag 后等待真实 PUT response 并 reload 回显。
- 2026-06-12 Dashboard management DONE 只覆盖管理列表 lifecycle: filter/list load, row action visibleWhen, publish/unpublish/delete, row/edit/create navigation, and All/Personal/Global tabs. `page.request.post/delete/get/put` 在该 spec 中用于 fixture create/cleanup/readback/add-widget and API state setup;核心完成证据来自浏览器 row action 点击后等待真实 publish/unpublish/delete response、API status readback、UI reload 回显与 row gone 断言。旧 spec-level `PUT /api/pages/{pid}` schema patch 已移除;`dashboard_management_list` 的 `onRowClick/detailUrl` 已固化到 `test-fixtures.setup.ts`。
- 2026-06-12 Dashboard widget payload DONE 覆盖 37 个 widget 的 representative saved-payload matrix: `DW-037..DW-073` UI click-add、static dataSource edit、单个代表属性 edit、Save click、`/api/dashboards/{pid}` readback、exact type/componentType/default size/staticData/edited config。`page.request.get('/api/dashboards/{pid}')` 只用于保存后的后端读回证据;核心配置动作是浏览器 UI click-add、property panel edit、static dataSource edit 和 Save click。本 slice 首次红灯来自 4 个 title default mismatch,修正为 registry/helper 的真实 click-add 契约后 `37 passed`;这是契约校准,不是 flaky。2026-06-12 Dashboard widget configSchema DONE 覆盖 `DW-074-01..37`:从 `widgetDefinitions` registry 直接枚举当前 37 个 widget 的 170 个 `configSchema` 字段,真实浏览器添加每个 widget 后断言 `widget-prop-*` 可见,并对 `dataSource.modelCode/queryCode` 验证 aggregate/namedQuery `dependsOn` branch 的隐藏/显示。该 slice 首次红灯来自 `icon` schema 缺 `widget-prop-icon`,产品修复为 `WidgetPropertyPanel` 给 `IconPicker` 外层加 schema-keyed testid。它不覆盖每个字段的保存语义、runtime visual/semantic rendering 或 workbench live-data semantics。
- 2026-06-12 Dashboard widget runtime DONE 现在覆盖全 37 个 widget 的 published viewer runtime render path:`DWR-001` / `DWR-004` / `DWR-005` / `DWR-006` / `DWR-007` / `DWR-008` / `DWR-009` / `DWR-010` / `DWR-011` / `DWR-012` / `DWR-013` 先覆盖 representative display/data/embed/workbench/CRM/BPM widgets,`DWR-014` 再覆盖剩余 foundational chart widgets: `smart-number-card`, `smart-bar-chart`, `smart-line-chart`, `smart-pie-chart`, `smart-area-chart`, `smart-funnel-chart`, `smart-scatter-chart`, `smart-radar-chart`, `smart-table-chart`, `smart-gauge-chart`, `smart-heatmap-chart`, `smart-treemap-chart`, `smart-map-chart`。`page.request.post('/api/dashboards')` / `post('/publish')` / `delete` 只用于 deterministic fixture create/publish/cleanup,因为本轮验证对象是 published Dashboard Viewer consumer runtime,不是设计器 UI authoring;核心证据来自浏览器打开 `/dashboards/view/{code}` 后的 `data-widget-type`、业务文本、DOM 属性、workbench testid、loading placeholder exit、chart surface、gallery/kanban content、number value、table headers/rows、map placeholder SVG、recent link DOM、announcement title/content/pinned/urgent badge DOM、inbox row/type badge DOM、calendar red dot DOM、CRM pipeline stage row、CRM lead company/contact、CRM activity subject、CRM canonical navigation、CRM empty/unavailable/variant/permission-denied DOM、BPM process row/filter/navigation、BPM running stats 和 browser-triggered CRM/BPM API responses。page-golden-audit 在本 checkout 缺失,因此 dashboard runtime slices 不声明该 gate 通过;已有 `E2E-REQUEST-RATIO`/API-heavy warning 均分类为 setup-heavy runtime fixture 警告,不是 API 替代 viewer DOM 证据。它仍不覆盖每个 config 字段的保存/runtime semantic effect。
- 2026-06-12 Dashboard widget runtime 首次红灯来自测试 fixture shape: content/embed widgets 未携带 static `dataSource`,触发 legacy `normalizeWidget` fallback 后把 `visualization.content/src/targetDate` 留在嵌套对象内,组件正确进入未配置态。修正为当前 saved-payload 契约 shape 后 `DWR-001` 通过;这不是产品 flaky。
- 2026-06-12 `DWR-002` 只覆盖 `smart-quick-note` viewer persistence seam: spec 先用 `page.request.get('/api/user-notes')` 读取原始内容用于 cleanup restore,然后在浏览器中打开 published dashboard、填充 `quick-note-textarea`、blur 触发真实 `PUT /api/user-notes`、断言保存反馈、reload 后断言 textarea value 持久化。`page.request.get` 是恢复原值的夹具读回,不是替代 UI 保存;核心完成证据来自 UI fill/blur、browser-triggered PUT response 和 reload readback。
- 2026-06-12 `DWR-002` 首次红灯来自断言只接受英文 `Just saved|Last saved`,而当前 locale 渲染 `刚刚保存`;修正为包含当前中文保存反馈后通过。这是 i18n 断言校准,不是产品 flaky。
- 2026-06-12 `DWR-003` 只覆盖 `smart-shortcuts` authored static shortcut interaction seam: deterministic dashboard fixture 携带 `config.shortcuts`,浏览器打开 published viewer 后断言 `shortcuts-list`、目标 link href,点击后断言 URL 变为 `/dashboards`。`page.request.post('/api/dashboards')` / publish / delete 仍只用于 fixture 生命周期,核心完成证据来自 viewer link DOM 和浏览器导航。
- 2026-06-12 `DWR-003` 首次红灯是产品 seam: `dashboardService.normalizeWidget` 的 dataSource branch 没有 passthrough `shortcuts`,导致 published viewer 收到默认快捷入口而不是 authored shortcut rows。新增 `dashboardService.test.ts` 红绿锁定该契约,产品修复为把 `shortcuts` 加入 passthrough whitelist 后 unit + E2E 通过。
- 2026-06-12 `DWR-004` 只覆盖 advanced representative runtime widgets: deterministic dashboard fixture 携带 static dataSource / static visualization props,浏览器打开 published viewer 后用 `data-widget-type` 定位消费方 block,对 `smart-wordcloud-chart` / `smart-combo-chart` / `smart-nps-chart` 断言 ECharts root/canvas/SVG 非零渲染面且无 loading/config/error 文案,对 `smart-gallery` 断言 authored title/description/image alt,对 `smart-kanban` 断言 `Backlog` / `Done` 分组列和 `Runtime Card A/B` 卡片。`page.request.post('/api/dashboards')` / publish / delete 仍只用于 fixture 生命周期,核心完成证据来自 viewer DOM/chart surface。
- 2026-06-12 `DWR-004` 首次红灯是 test oracle 问题:图表 block 内同时存在 action-menu 小 SVG,原 surface query 选中 16px 菜单图标而不是图表画布。修正为只在 `[data-widget-type]` 下查 `.echarts-for-react` / `canvas` / `svg` 并取最大面积元素后通过;没有改产品代码。
- 2026-06-12 `smart-recent` runtime 已由 `DWR-005` 计入 representative DONE,但边界只覆盖 engagement API recent-page 数据进入 published viewer 后的 link title/href 渲染。此前未计数的 smart-recent probe 被拆成后端契约红绿、前端缓存稳定性和 recent pruning 稳定性: `UserEngagementControllerIntegrationTest` 证明 `POST -> GET recent_view` 同用户/租户读回成立,并修复 `ASSIGN_ID Long` `id` 以 JSON number 返回的 JS 精度风险;`DWR-005` 通过清理 localStorage recent cache、等待 viewer 的浏览器 `GET /api/user-engagement` 响应、再断言 DOM,避免把缓存旧值误判为产品缺陷。本轮额外 API probe 证明新记录曾因历史 `createdAt=null` rows 被 prune 误删;`UserEngagementServiceImplTest` RED/GREEN 固化 created/updated timestamp fill 与 null-timestamp-history-first pruning,最终 full runtime spec `13 passed`。
- 2026-06-12 `smart-announcement` runtime 已由 `DWR-006` 计入 representative DONE,但边界只覆盖 active pinned urgent announcement API data 进入 published viewer 后的 title/content/pinned/urgent DOM 渲染。`page.request.post('/api/announcements')` / `GET /api/announcements` / `DELETE` 只用于 deterministic fixture setup/readback/cleanup;后端 `AnnouncementControllerIntegrationTest` 证明 `POST /api/announcements` 返回 JS-safe string `id/publishedBy`,且 `POST -> GET /api/announcements` 同租户 active announcement 读回成立。
- 2026-06-12 `smart-inbox` / `smart-calendar` runtime 已由 `DWR-007` 计入 representative DONE,但边界只覆盖当前浏览器用户的 pending approval inbox API data 进入 published viewer 后的 inbox row/type badge 与 high-priority calendar red dot 渲染。`page.request.post('/api/test/fixture')` / `/api/dashboards` lifecycle / `PUT /api/inbox/{id}/dismiss` 只用于 deterministic fixture setup/readback/cleanup;核心证据来自 viewer 的浏览器 `GET /api/inbox` 响应与 DOM。它不覆盖 inbox approve/dismiss UI 交互、calendar month navigation 或其它 inbox item type 的视觉映射。
- 2026-06-12 `smart-pipeline` / `smart-leads` / `smart-activities` runtime 已由 `DWR-008` / `DWR-010` / `DWR-011` / `DWR-012` / `DWR-013` 计入 representative DONE,但边界只覆盖 CRM starter live-data + canonical navigation + empty/unavailable frontend states + frontend stage/status/type variant rendering + permission-denied frontend states: command fixture -> API readback -> published viewer -> browser-triggered `/api/workbench/pipeline`、`/api/dynamic/crm_lead/list`、`/api/dynamic/crm_activity/list` -> DOM render,stage/lead/activity click -> `/p/...` route,deterministic route fulfillment 覆盖六个 pipeline stages、四个 lead statuses、四个 activity types,以及 deterministic 403 response injection 覆盖 `pipeline-permission-denied` / `leads-permission-denied` / `activities-permission-denied`。`page.request.post('/api/plugins/import/import-directory-sync')` / command APIs / dashboard lifecycle / dynamic list readback 只用于 deterministic fixture setup/readback/cleanup;`DWR-011` / `DWR-012` / `DWR-013` 的 `page.route` 只用于 deterministic 空态/CRM unavailable/variant/permission response injection,不替代 `DWR-008` 的 live-data happy path。
- 2026-06-12 `smart-my-process` / `smart-process-stats` runtime 已由 `DWR-009` 计入 representative DONE,但边界只覆盖 BPM workbench live-data seam: minimal BPMN create/deploy -> SmartEngine process start -> `/api/bpm/workbench` 和 `/api/workbench/bpm-stats` readback -> published viewer -> browser-triggered BPM API responses -> DOM render/filter/click-to-status。`page.request.post('/api/bpm/process-definitions')` / deploy / start / terminate / dashboard lifecycle 只用于 deterministic fixture setup/readback/cleanup;核心证据来自 viewer DOM、running filter、process row click navigation 和 browser-triggered BPM API responses。它不覆盖 task-center approval interaction、all process statuses/permissions, or BPM designer runtime branch matrix。
- 2026-06-12 Dashboard maintained smoke 使用 `dashboard-tab-reorder.spec.ts -g DTR-001` 作为 `/dashboards` viewer 基础加载证据。旧 `dashboard.spec.ts -g F3-E01` 仍指向 `/reports/overview` 并失败为 pre-existing stale route,不计入本 slice 的产品失败或完成证据。
- 2026-06-12 Report table Excel artifact DONE 只覆盖 saved `extension.reportDsl` 中 table block + static dataSource → `POST /api/reports/export/excel` → BFF binary proxy → browser download → XLSX parse 的链路。`page.request.post('/api/pages')` 只用于创建 deterministic report fixture,`page.request.delete` 只用于清理;核心完成证据来自真实 Report Designer 页面点击 Export Excel 后的 download 和 workbook 解析。
- Report Excel 第一次 E2E 红灯不是测试问题:旧 BFF JSON proxy 将 OOXML bytes 重序列化成 JSON string(`"PK...`),导致下载文件损坏。本轮产品修复为 binary route detection 覆盖 `/api/reports/export/excel` 并保留 POST method/body;该 seam 已由 BFF unit + E2E 双证据覆盖。
- 2026-06-12 Report table PDF artifact DONE 只覆盖 saved `extension.reportDsl` 中 table block + static dataSource → `POST /api/reports/export/pdf` → BFF binary proxy → browser download → PDF header/page marker 的链路;后端单测额外用 `PDFTextStripper` 解析标题、表头和数据行。`page.request.post('/api/pages')` 只用于创建 deterministic report fixture,`page.request.delete` 只用于清理;核心完成证据来自真实 Report Designer 页面点击 Export PDF 后的 download。
- 2026-06-12 Report non-table static artifact DONE 只覆盖 saved `extension.reportDsl` 中 static dataSource / static text 的语义投影:grouped-table 分组、cross-tab pivot、stat 聚合、rich-text 段落、chart category/value 聚合表、barcode/watermark/header/footer 文本 artifact。Excel 证据来自多 sheet workbook parse;PDF 证据来自后端 `PDFTextStripper` 与 E2E 解压 Flate content stream 后的关键文本断言。它不声明 chart/barcode/watermark 的视觉等价渲染。
- 2026-06-12 Report model/namedQuery/API Excel dataSource artifact DONE 覆盖 saved `extension.reportDsl` 中 `type:model` / `type:namedQuery` / canonical `type:api` dataSource:后端用 `DynamicDataService` model filter、`NamedQueryService` params、`/api/datasource(s)/list` + `datasourceId=nq:{queryCode}` delegation 解析 rows;E2E 通过真实浏览器点击 Export Excel、下载 XLSX、解析 `Model Orders` / `NamedQuery Orders` / `API Orders` 三个 sheet 的精确行。setup 里的 API request 只用于创建 deterministic e2et_order / namedQuery / report fixture;核心 artifact 证据不是 API export。
- 2026-06-12 Report JSON round-trip artifact DONE 覆盖 saved `extension.reportDsl` + resolved rows 的可迁移 JSON 包:后端返回 `format=auraboot.report.export.v1`、`reportPid`、完整 `reportDsl`、`dataSets`,E2E 通过真实浏览器点击 Export JSON、等待 POST `/api/reports/export/json` 和下载、解析 `.report.json`,并断言 model/namedQuery/API 三路 rows。setup 里的 API request 只用于创建 deterministic e2et_order / namedQuery / report fixture;核心 artifact 证据不是 API export。
- 2026-06-12 Report PDF page-settings/text-hierarchy fidelity DONE 覆盖 saved `page.size/orientation/margin(mm)` -> PDF `/MediaBox` 与 content stream 坐标/字号层级:`RPT-OP-13` 通过真实浏览器点击 Export PDF、等待 POST `/api/reports/export/pdf` 和下载、解析 `/MediaBox` 与 `Tf/Td`;后端 PDFBox 单测先红于固定 `48pt` origin,再由 margin mm->points 修复转绿。
- Report export 仍不能声明 all-formats/all-visual-equivalence 完成:JSON round-trip 与 PDF page-settings/text-hierarchy 已计入 DONE,但不覆盖 PDF chart/barcode/watermark 像素级视觉等价。
- 前一次 Dashboard/Report 合批失败的根因是后端 `bootRun` 被 SIGTERM 停止后前端重定向到登录页;该失败已通过重启后端、重跑 setup、分批 fresh run 排除,不计产品缺陷。本轮 Report non-table 首次 E2E 红灯根因是 Vite SSR 进程未设置 `SPRING_BOOT_URL/BFF_INTERNAL_URL` 而默认访问旧 `:6443`,修正隔离栈 env 后 targeted 与 Report Operations fresh run 通过。

### Phase 3: P0/P1 补测

优先顺序建议：

1. Automation Designer：因为和规则、SLA、BPM、权限联动最多。
2. Page Designer：17 个 block-type runtime inventory、table bulk edit/delete DB side effects、form-buttons create/update command side effects、form-section required/readonly/visibleWhen generic semantics、custom block runtime host/loader/error surface、sub-table CRUD/aggregate row、sub-table foreignKey/resolveVia/dataSource modes、layout/container runtime nested tabs/grid/stack 已完成;下一轮不要继续补同一层,应转向更深的 Page Designer component-family gaps: layout/container designer authoring nesting/move/delete/undo/property combinations, upload/download artifact, text/number/reference/select/date field type variants, custom block props schema/valueField/property-panel save semantics, and property-panel combinations。
3. Dashboard / Report Designer：Dashboard tab persistence、management row action/list tab lifecycle、Dashboard Excel/PDF artifact、37 widget add/property presence + representative saved payload、37 widget exhaustive configSchema field presence + dependsOn branch matrix、37 widget published viewer runtime render path、quick-note runtime persistence、shortcut runtime navigation、recent-visit runtime data rendering、announcement runtime data rendering、inbox/calendar runtime data rendering、CRM pipeline/leads/activities runtime data + navigation + empty/unavailable state + stage/status/type frontend variants + permission negative states、BPM my-process/process-stats live-data rendering、Report table + non-table static Excel/PDF artifact、Report model/namedQuery/API Excel dataSource artifact、Report JSON round-trip artifact、Report PDF page-settings/text-hierarchy fidelity 已完成;下一轮不再补同一层 runtime render,应转向 Page Designer component-family gaps 或 Dashboard 每个 config 字段的保存/runtime semantic effect,Report PDF chart/barcode/watermark 像素级视觉等价仅在后续明确纳入范围时推进。
4. BPMN Designer：当前证据较强，优先补 gateway/callActivity/runtime 细节缺口和旧 suite 治理。

### Phase 4: 验证与收口

每个 slice 至少跑：

- `pnpm typecheck`
- targeted Playwright spec
- 对应后端 targeted test，如涉及 runtime / deploy / evaluator
- `/e2e-truth` grep
- `git diff --check`
- 文档回填命令和结果

## 验证命令模板

```bash
cd /Users/ghj/work/auraboot/auraboot/.worktrees/decisionops-console-completion/web-admin

pnpm typecheck

NO_PROXY=localhost,127.0.0.1 \
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 \
BACKEND_URL=http://127.0.0.1:6443 \
pnpm exec playwright test -c playwright.quick.config.ts \
  tests/e2e/<target-spec>.spec.ts \
  --project=chromium
```

后端 runtime 涉及 BPM / Automation / Decision 时，补对应 Gradle targeted test，不能只跑前端。

## 不可接受的完成声明

以下都不能算完成：

- “全部 pass”但还有 `test.skip` / `test.fixme` 包装产品缺口。
- 只验证按钮 visible，不点击。
- 点击保存只看 toast，不等 API，不反查 DTO/schema。
- 导出只看按钮，不验证 download、文件名、header、解析内容。
- 用 API PUT 改 schema 代替 UI 属性面板。
- shared kernel 测过，就说 Page/BPMN/Automation/Dashboard 都测过。
- 用 `>=1` 证明具体组件类型已覆盖。
- 用 fixed sleep 证明异步 runtime 没发生。
- 只跑 UI，不验证后端 runtime 或 artifact。

## 新窗口交接摘要

当前已完成的前置事实：

- 规则中心收口文档：`docs/backlog/2026-06-11-rule-center-integration-coverage-gap.md`
- 已关闭规则中心 targeted slice：EventPolicy、Automation rule binding、BPM rule binding、Permission ABAC、SLA rule binding、DMN editor deepening、FEEL built-ins、Rollout long-window、Dashboard Excel/PDF artifact。
- 已退役旧 BPMN 假 deep 证据：
  - `designer/bpmn-node-properties.spec.ts`
  - `designer/bpmn-designer-deep.spec.ts`
- 已收紧 Automation legacy deep：
  - 删除 `AT-005` 条件 builder skip 假覆盖。
  - 删除不存在产品契约的 `AT-014 priority` 假覆盖。
  - 收紧 `AT-011/AT-012` API 反查。
  - 替换 validation gate fixed wait。

新窗口不要重复做这些已完成项；应从本文档 Phase 0 开始，先生成完整 inventory 和矩阵，再按 P0/P1 风险补测。
