# ArchUnit configuration
# FreezingArchRule stores known violations here — commit this directory
freeze.store.default.path=src/test/resources/archunit_store
# Allow creating the store on first run
freeze.store.default.allowStoreCreation=true
# Allow violations to decrease (auto-remove fixed violations from the store)
freeze.store.default.allowStoreUpdate=true
