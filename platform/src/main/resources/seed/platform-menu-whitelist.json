{
  "description": "Menu codes allowed to exist in ab_menu with plugin_pid IS NULL. Any orphan menu not listed here will cause startup to fail. Keep this list empty unless a menu is truly platform-level (no plugin can own it, and all tenants need it).",
  "allowedCodes": []
}
