{
  "_comment": "Deliberate exceptions to check-no-secret-echo. An entry here is a decision, not an oversight — state why the secret is allowed to reach the output stream.",
  "exemptions": [
    {
      "file": "scripts/seed-acp-runtime-test.sh",
      "reason": "Local-dev seeder whose whole purpose is to hand you a throwaway test JWT to paste into your shell ('export ACP_TOKEN=...'). The token is minted against a local stack, is short-lived, and printing it IS the feature. Never point this script at a shared or production backend."
    },
    {
      "file": "scripts/run-bpm-release-image-gate.sh",
      "reason": "LOCK_TOKEN is a run-local lock-ownership nonce written to $LOCK_DIR/owner, never to stdout/stderr; cleanup removes the lock only while the file content still matches, so concurrent CI jobs cannot steal each other's release-image lock."
    }
  ]
}
