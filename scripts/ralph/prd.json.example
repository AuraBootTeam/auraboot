{
  "project": "AuraMeta",
  "branchName": "ralph/feature-name",
  "description": "Brief feature description",
  "userStories": [
    {
      "id": "US-001",
      "title": "Add database table and mapper",
      "description": "Create the ab_xxx table with required columns and MyBatis Mapper interface",
      "acceptanceCriteria": [
        "Table ab_xxx exists with columns: id, name, code, tenant_id, created_at, updated_at, deleted_flag",
        "XxxMapper interface with basic CRUD methods",
        "Unit test for Mapper.insert() passes",
        "compileJava passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": "Check existing table patterns: psql -c '\\d ab_meta_model'"
    },
    {
      "id": "US-002",
      "title": "Add service layer with business logic",
      "description": "Create XxxService with validation and CRUD operations",
      "acceptanceCriteria": [
        "XxxService with create/update/delete/list methods",
        "Input validation for required fields",
        "Unit tests for service methods pass",
        "compileJava passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": "Follow existing service patterns in platform/src/main/java/.../service/"
    },
    {
      "id": "US-003",
      "title": "Add REST API controller",
      "description": "Create XxxController exposing CRUD endpoints",
      "acceptanceCriteria": [
        "POST /api/xxx — create",
        "PUT /api/xxx/{id} — update",
        "DELETE /api/xxx/{id} — delete",
        "GET /api/xxx/list — list with pagination",
        "Controller unit tests pass",
        "compileJava passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": "Use @RestController, follow existing controller patterns"
    },
    {
      "id": "US-004",
      "title": "Add DSL page configuration",
      "description": "Create page designer config for list + form views via plugin JSON",
      "acceptanceCriteria": [
        "Plugin JSON with model, fields, commands, pages defined",
        "List page shows data with correct columns",
        "Form page allows create/edit with validation",
        "NO_PROXY=localhost npx playwright test xxx.spec.ts passes"
      ],
      "priority": 4,
      "passes": false,
      "notes": "DSL-first: use plugin config, NOT tsx. Check plugins/schemas/plugin-manifest.schema.json"
    }
  ]
}
