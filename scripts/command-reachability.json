{
  "$comment": "A declared command with no UI entry point cannot have UI coverage. Exceptions must carry a reason. See scripts/check-command-reachability.mjs.",
  "roots": [
    "plugins"
  ],
  "exemptPrefixes": [
    "seed_",
    "internal_"
  ],
  "allow": {
    "crm": {
      "crm:detail_account": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_accounts": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_activity": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_activities": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:create_activity_relation": "Embedded timeline relation API used by nested activity composers; there is no standalone relation page.",
      "crm:delete_activity_relation": "Embedded timeline relation API used by nested activity composers; there is no standalone relation page.",
      "crm:list_activity_relations": "Embedded timeline relation query used by nested activity composers; there is no standalone relation page.",
      "crm:detail_approval_case": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_approval_cases": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:list_assignment_rules": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_assignment_rule": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_campaigns": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:update_campaign_member": "Campaign members are managed inside the campaign detail sub-table, not through a standalone model page.",
      "crm:list_campaign_members": "Campaign members are read inside the campaign detail sub-table, not through a standalone model page.",
      "crm:update_campaign_metric": "Campaign metrics are synchronized aggregate facts and are not manually editable in the CRM UI.",
      "crm:detail_clarification": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_clarifications": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_complaint": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_complaints": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_contact": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_contacts": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_customer_request": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_customer_requests": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_email_log": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_email_logs": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_email_template": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_email_templates": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_lead": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_leads": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:list_lead_capacities": "Agent/API query surface; the dynamic capacity list page reads the standard data endpoint.",
      "crm:list_lead_pools": "Agent/API query surface; the dynamic policy list page reads the standard data endpoint.",
      "crm:detail_lead_pool": "Agent/API query surface; the dynamic policy detail page reads the standard data endpoint.",
      "crm:list_lead_pool_items": "Agent/API query surface; the operational workbench uses governed named queries while the batch page reads the standard data endpoint.",
      "crm:list_lead_pool_recycle_rules": "Agent/API query surface; the dynamic recycle-rule list page reads the standard data endpoint.",
      "crm:detail_lead_pool_recycle_rule": "Agent/API query surface; the dynamic recycle-rule detail page reads the standard data endpoint.",
      "crm:list_score_rules": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_score_rule": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:detail_opportunity": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_opportunities": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:list_opp_contacts": "Opportunity contacts are read inside the opportunity detail sub-table, not through a standalone model page.",
      "crm:list_opp_lines": "Opportunity lines are read inside the opportunity detail sub-table, not through a standalone model page.",
      "crm:detail_quote_summary": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_quote_summaries": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_review": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_reviews": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:detail_risk": "Agent/API query surface; the dynamic detail page reads the standard data endpoint.",
      "crm:list_risks": "Agent/API query surface; the dynamic list page reads the standard data endpoint.",
      "crm:create_sla_breach": "SLA breaches are immutable evaluator-generated incidents; users inspect them through the read-only breach list.",
      "crm:list_sla_breaches": "Agent/API query surface; the read-only dynamic list page reads the standard data endpoint.",
      "crm:list_sla_policies": "Agent/API query surface; the dynamic list page reads the standard data endpoint."
    }
  },
  "baseline": {
    "agent-control-plane": [
      "acp:create_agent_observation",
      "acp:create_agent_run",
      "acp:delete_agent_observation",
      "acp:delete_agent_run",
      "acp:delete_mcp_server",
      "acp:delete_object_alias",
      "acp:delete_semantic_term"
    ],
    "asset-management": [
      "tasset:detail_asset",
      "tasset:detail_category",
      "tasset:detail_maintenance",
      "tasset:list_assets",
      "tasset:list_categories",
      "tasset:list_maintenance",
      "tasset:report_repair",
      "tasset:update_asset",
      "tasset:update_category",
      "tasset:update_maintenance"
    ],
    "core-site-key": [
      "behavior_site_key:create"
    ],
    "golden-path": [
      "gp:approve",
      "gp:detail_task",
      "gp:list_approvals",
      "gp:list_task_comments",
      "gp:list_tasks",
      "gp:reject",
      "gp:request_approval",
      "gp:update_task",
      "gp:update_task_comment"
    ],
    "hr-essentials": [
      "thr:detail_attendance",
      "thr:detail_employee",
      "thr:detail_leave_request",
      "thr:list_attendance",
      "thr:list_employees",
      "thr:list_leave_requests",
      "thr:update_attendance",
      "thr:update_employee",
      "thr:update_leave_request"
    ],
    "org-management": [
      "org:create_department",
      "org:create_employee",
      "org:create_position",
      "org:create_team",
      "org:delete_department",
      "org:delete_employee",
      "org:delete_position",
      "org:delete_team",
      "org:update_department",
      "org:update_employee",
      "org:update_position",
      "org:update_team"
    ],
    "page-manager": [
      "pgm:update_page_schema"
    ],
    "platform-admin": [
      "admin:create_member"
    ],
    "project-management": [
      "tpm:detail_milestone",
      "tpm:detail_project",
      "tpm:detail_task",
      "tpm:list_milestones",
      "tpm:list_projects",
      "tpm:list_tasks",
      "tpm:update_milestone",
      "tpm:update_project",
      "tpm:update_task"
    ],
    "showcase": [
      "sc:create_monthly_metric",
      "sc:create_showcase",
      "sc:delete_monthly_metric",
      "sc:delete_showcase",
      "sc:detail_showcase",
      "sc:list_showcase",
      "sc:update_showcase"
    ],
    "simple-inventory": [
      "tinv:detail_product",
      "tinv:detail_stock_in",
      "tinv:detail_stock_out",
      "tinv:detail_warehouse",
      "tinv:list_products",
      "tinv:list_stock_in",
      "tinv:list_stock_out",
      "tinv:list_warehouses",
      "tinv:update_product",
      "tinv:update_stock_in",
      "tinv:update_stock_out",
      "tinv:update_warehouse"
    ],
    "test-fixtures": [
      "e2et:touch_order_noperm",
      "e2eto:create_e2et_order"
    ],
    "workflow-demo": [
      "wd:create_leave_balance",
      "wd:delete_leave_request",
      "wd:update_leave_balance",
      "wd:update_leave_request"
    ]
  }
}
