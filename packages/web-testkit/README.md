# @auraboot/web-testkit

Shared Playwright helpers for asserting immutable AuraBoot application identity—including the application lock, source commit, and release image digest—and opening composed routes.
The package is test-only; applications consume it as a development dependency and provide their own Playwright runtime.
