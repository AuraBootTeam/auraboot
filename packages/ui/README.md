# @auraboot/ui

Public UI primitives for AuraBoot Web Shell and independently released product contributions.
React and React DOM are peer dependencies so an assembled application owns exactly one runtime.
