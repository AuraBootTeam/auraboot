/**
 * Settings Module
 *
 * Settings panel for configuring page, editor, and appearance.
 *
 * @since 3.2.0
 */

export { SettingsPanel } from './SettingsPanel';
export type { SettingsPanelProps } from './SettingsPanel';

export type {
  SettingsCategory,
  PageSettings,
  EditorSettings,
  AppearanceSettings,
  ExportSettings,
  AllSettings,
} from './types';

export { DEFAULT_SETTINGS, SETTINGS_CATEGORY_INFO } from './types';
