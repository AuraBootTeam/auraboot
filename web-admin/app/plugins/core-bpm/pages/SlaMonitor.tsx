/**
 * SLA Monitor page — SLA monitoring dashboard.
 * Migrated from app/routes/bpm/sla-monitor.tsx (M3 core-bpm extraction).
 */
import { SlaMonitorPanel } from '~/bpm/components/SlaMonitorPanel'

export default function SlaMonitorPage() {
  return <SlaMonitorPanel />
}
