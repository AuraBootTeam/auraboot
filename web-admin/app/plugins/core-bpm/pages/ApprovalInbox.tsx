/**
 * Approval Inbox page — BPM approval task management.
 * Migrated from app/routes/bpm/approval-inbox.tsx (M3 core-bpm extraction).
 */
import { ApprovalInbox } from '~/plugins/core-bpm/components/ApprovalInbox'

export default function ApprovalInboxPage() {
  return <ApprovalInbox />
}
